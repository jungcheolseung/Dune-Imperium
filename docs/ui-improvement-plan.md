# UI 개선 계획

기준일: 2026-09-21

브라우저 플레이 UI(`src/dune_imperium/server/static/`)의 개선 순서를 정리한다.
근거는 2026-09-20 세션의 10개 관점 감사(73 에이전트 → 반박 검증 32건 확정 /
29건 기각)와 같은 세션의 직접 실측이다. 규칙 근거는 [`rules/README.md`](rules/README.md),
마일스톤은 [`implementation-plan.md`](implementation-plan.md)를 따른다.

사용자 결정(2026-09-20):

- 1순위는 **`app.js` 구조 정리**.
- 화면 언어는 **혼용 없이 언어 설정(ko/en)을 따로** 둔다. 고유명사(카드·리더·
  공간 이름)는 영어를 유지하고, **룰북에 명시된 게임 용어**만 한글/영어를 가른다.
  용어는 KR·EN 룰북 대조로 만든 용어집에서만 가져온다.
- 카드 인쇄 텍스트는 **영어를 유지하고 용어 툴팁을 얹는다**(아래 "언어 정책").

## 실측 기준선 (2026-09-20)

`ui-verify` 서버(포트 8765), 시스템 Chrome, base 및 Immortality 판.

| 항목 | 1600×1000 | 1440×900 |
| --- | --- | --- |
| `#board` 폭 / 보드 스캔 축소 | 576px / **10.4배** | 470px / **12.8배** |
| `#action-log` 폭 | 529px (34%) | 475px |
| `#seats` 내용 대 칸 | 1082px / 752px → **넘침** | 933px / 652px → **넘침** |
| `#market` 내용 대 칸 (base) | 912px / 752px → **넘침** | 맞음 |
| `#market` 내용 대 칸 (전 확장) | **1630px / 751px (2.2배)** | 1630px / 519px |
| Bene Tleilax board | **167×119px, 33.3배 축소** | 화면 밖 |

- 전 확장에서 공용 카드 17장 중 **10장이 화면 밖**이고, 합법 목표를 화면 안으로
  스크롤해 주는 코드는 없다.
- Bene Tleilax board의 연구 칸은 19×21px, 좌석 토큰은 9.7px다.
- 좌석 패널은 어느 해상도에서도 4장이 다 들어가지 않는다(네 번째가 항상 밖).
- 좌석당 view 필드는 **67개**다. 데이터가 없는 게 아니라 고르지 않은 것이다.

## 계획을 바꾼 발견 3가지

### 1. 언어 문제는 문구가 아니라 아키텍처다

`ICON_RULES`(`static/render.js:51`)는 **영어 정규식 29개**를 렌더된 산문에 돌린다
(`solari`, `Draw N cards`, `troops`, `Persuasion`, `Trash`, `Discard`,
`Signet Ring` …). 결과가 둘이다.

- `trash → 폐기`, `draw → 뽑기`로 바꾸는 순간 전부 매칭 실패 → **아이콘이 통째로
  사라진다.** 언어 설정의 실질적 장벽이다.
- 규칙 27번 `/\bSignet Ring\b/`가 *카드 이름*에 발동해 로그가
  `배치 — [아이콘], Arrakeen`으로 렌더된다(아이콘에 `alt`는 있으므로 텍스트가
  사라지는 것은 아니지만, 카드 이름 자리에 그림이 온다).

**용어집과 아이콘 레이어는 같은 작업이다.**

### 2. 번역 질량은 결정 prompt가 아니라 카드 텍스트에 있다

기본 룰셋 카탈로그만으로 **549개 문자열 / 30,394자, 한글 0**이다(카드 능력 396 ·
Conflict 보상 54 · Contract condition/reward 56 · Leader ability/signet 38 ·
notes 5). 감사에서 세 관점이 "가장 많이 읽는 문자열"로 지목한 결정 prompt
(약 50개, `rules/*.py`의 `prompt=` 55개 자리 중 리터럴 36 · 동적 13)는 그중
**5%**뿐이다.

그런데 이 텍스트는 `SourceDocument.CARD_FACE` **전사물**이고 감사 문서가 지킨다.
그래서 "언어 설정"은 단일 정책이 될 수 없고 아래처럼 갈라야 한다.

### 3. "구조" 문제의 체감 증상은 스크롤이다

`SCROLL_PANES`(`static/render.js:212`)는
`["seats", "side-main", "board", "market", "private-zone"]`으로 **`side`가 없다.**
그런데 `style.css:1127`의 `@media (max-width: 1700px)`가 `#side`를 스크롤러로
만들고(`overflow-y: auto`) `#side-main`을 `overflow: visible`로 바꾼다.

**1700px 미만 모든 화면에서 다른 좌석이 한 수 둘 때마다 사이드 칼럼이 맨 위로
점프한다.** [`multiplayer-design.md`](multiplayer-design.md)가 약속한 G7 위반이고,
`scripts/e2e/open_mode.py:282`의 검사는 자기 뷰포트가 넓어서 공허하게 통과한다.

## 언어 정책 (사용자 결정 2026-09-20)

| 대상 | 정책 |
| --- | --- |
| UI 크롬(버튼·패널 제목·상태 문구) | 언어 설정을 따른다 |
| 결정 prompt, 행동 로그 라벨 | 언어 설정을 따른다 |
| 카드·리더·공간·Conflict·Contract **이름** | **영어 고정**(정식 한글 번역본 대조 전) |
| 카드/Conflict/Contract/Leader **인쇄 텍스트** | **영어 고정 + 용어 툴팁** |
| 룰북에 명시된 게임 용어 | 용어집이 정하는 ko/en |

카드 인쇄 텍스트를 번역하지 않는 이유:

1. `SourceDocument.CARD_FACE` 전사물이라 번역하면 전사 원칙과 감사 문서가 깨진다.
2. 사용자가 가진 물리 카드와 화면이 어긋난다.
3. **한글 카드 이미지를 확보하면 그림은 코드 변경 없이 한글화된다** —
   `display/images.py:40`이 이미 `DEFAULT_LANGUAGES = ("ko", "en")`로 한글 스캔을
   먼저 찾고 영어로 폴백한다. 지금 카탈로그 URL이 `/card-images/en/...`인 것은
   한글 스캔이 없기 때문이지 코드가 영어에 묶여서가 아니다.

용어는 **발명하지 않는다.** [`lessons.md`](lessons.md)에 trash/discard를 넘겨짚어
생긴 실제 버그가 기록돼 있다. KR 룰북이 실제로 쓰는 단어만 쓴다.

## 0단계 — 발견된 결함 3건 (2026-09-20 완료)

| | 내용 | 위치 |
| --- | --- | --- |
| a | Tech Module만 켜고 게임 생성 시 **HTTP 500 `Internal Server Error`**. `RulesetConfig.__post_init__`의 `ValueError`가 `_http_errors`를 통과한다 → `ValueError → 400`으로 잡고 메시지를 실어 보낸다 | `server/app.py`의 `_http_errors` |
| b | 내부 frame 식별자 노출(`· frame: turn`) 제거 | `static/render.js`의 `renderBanner` |
| c | 로그의 복수형 id 목록이 raw로 샌다(`Leader Ids: staban_tuek,…`). `logEventPayload`가 단수 id 키만 처리한다 | `static/panels.js:370`의 `logEventPayload` |

(c)는 Leader draft가 기본값이라 **모든 게임의 첫 로그 줄**에 나온다.

검증: `scripts/e2e/`의 `open_mode.py`·`staged_turn.py`·`spectate.py` 재실행.
(a)는 `tests/server/`에 회귀 테스트를 더한다(pytest로 잡히는 유일한 항목).

**완료** (`e119e46`, `9ad978f`). (a)는 `SessionError`가 이미 `ValueError`
하위 클래스라 핸들러를 넓히면 진짜 버그까지 400으로 가려지므로 config를 만드는
자리에서 변환했다. (c)는 계획보다 넓었다 — 감사는 복수형 `_ids`만 지목했으나
엔진의 payload id 키는 약 35종인데 허용 목록에 5종뿐이어서 `leader_id`·`tech_id`·
`contract_id`·`skill_id`가 전부 raw로 새고 있었다. `_id`/`_ids` 전체로 일반화하고
`action_id`는 `ACTION_LABELS`를 태운다.

## 1단계 — 구조 정리 (사용자 1순위)

- **1a. 스크롤·포커스 보존을 선언적으로 — 완료** (`5d2adbe`).
  실측으로 메커니즘을 확정했다: 1800px에서는 `#side-main`·`#side-log`가,
  1600·1366px에서는 `#side`가 스크롤러다 — 옛 목록은 1700px 초과에서만 맞았다.
  셋을 다 넣고 `keepScroll()`이 렌더 시점에 각 칸에 묻는다. 로그도 같은 증상이라
  (맨 위로 올려 둔 독자가 다음 렌더에 34,013px 끌려갔다) 끝에 있던 독자만 따라가게
  했다. e2e는 "스타일시트가 스크롤을 허용하는 모든 칸이 `SCROLL_PANES`에 있는가"로
  바꿔 **부류**를 잡고, 옛 클라이언트에 돌려 실제로 실패하는지 확인했다
  (`open_mode.py`의 옛 단언은 `#side-main`을 지목해 1600px에서 공허하게 통과했다).
- **1b. `app.js` 분할 — 완료** (`3485d25`). 섹션 배너를 따라 `core`·`screens`·
  `session`·`review`·`render`·`turn`·`board`·`panels`와 bootstrap만 남은 `app.js`
  아홉 개로 잘랐다(최대 `board.js` 1,271줄). 자르기 전후로 연속·무손실을 검증했고
  비어 있지 않은 5,064줄이 동일하다. 각 파일에 `"use strict"`를 둔다(스크립트 단위다).
  `races.py --ab`가 `**/static/app.js`를 route해 패치하고 있었는데 그 코드가
  `session.js`로 옮겨가 조용히 깨졌다(route handler의 assert는 `goto` 타임아웃으로만
  보인다) — route 대상을 옮기고 A/B가 다시 정직하게 동작함을 확인했다.
  아래는 착수 전 근거다.
  **classic script로 쪼개면 전역 계약이 그대로라 e2e 무변경·위험 0**이다
  (e2e가 `page.evaluate`로 잡는 것: `state` 76회, `applyAction`, `stagedTurn`,
  `playback`, `LIT_SPACES`, `refreshFlight`, `doorbell`). 빌드 도구가 없고
  `/static`은 단순 `StaticFiles` mount라 인프라 비용은 0이다. ES module은
  `window` 시험 표면을 따로 만들어야 하므로 지금은 하지 않는다.
- **1c. 라벨 테이블 통합 — 완료** (`7f333fa`). 일곱 개를 `static/labels.js`로
  바이트 동일하게 옮기고, 완전성 가드가 `app.js`를 지목하는 대신 `static/*.js`를
  훑도록 바꿔 이후 분할에도 꺼지지 않게 했다. 그 과정에서 확인: **엔진 event kind
  207개 중 177개에 한국어 라벨이 없다**(`prettify(kind)` 폴백) — 2단계 몫이다.
  아래는 착수 전 근거다. `PHASE_LABELS`(:56)·`FACTION_LABELS`(:67)·
  `ACTION_LABELS`(:76)·`EVENT_LABELS`(:308)·`EFFECT_ICON_LABELS`(:644)·
  `RESEARCH_BONUS_LABELS`(:4481)·`TLEILAXU_TRACK_LABELS`(:4495) 일곱 개를 한
  곳으로 모은다. 2단계의 토대다.

**하지 않는 것**: 로그 증분 렌더. 실측 80ms/911엔트리인데 함정이 넷이다
(그룹 tail 병합, fresh 부기, undo epoch, 역방향 review seek). 수지가 맞지 않는다.

참고 수치: `app.js`의 문자열 리터럴 1,972개 중 한글 포함 462개이고 그중
**329개(71%)가 한·영 혼용**이다(`"Agent 배치"`, `"카드 획득 (Intrigue)"`,
`"Feyd token 전진"`).

## 2단계 — 언어 (용어집 + 언어 설정)

**진행 상황 (2026-09-20).** 2a·2b 완료, 2c의 완전성 가드 완료, **언어 토글은
착수하지 않았다**(아래 "언어 토글을 미룬 이유").

| | 상태 | 커밋 |
| --- | --- | --- |
| 2a 용어집 | 완료 — 117행, 확장 포함 | `f8cbfe4`, `227f853` |
| 2b 아이콘 레이어 → 용어 토큰 | 완료 | `a137f5e` |
| 2b' 라벨의 한·영 혼용 제거 | 완료 — 혼용 195행 → 0 | `dafbf6c` |
| 2c 완전성 가드 | 완료 — action id·event kind·용어 3중 | `a137f5e`, `c7574f5` |
| 2c 언어 토글(ko/en) | **미착수** | — |

실측으로 확인한 성과:

- 로그에서 영어로 나오던 event 줄이 **전 확장 3판 3,572줄 중 1,438줄(40%) → 0줄**.
- 라벨 테이블의 한·영 혼용 **195행 → 0행**. 남은 영어 32행은 전부 카드·공간·
  Leader 능력 이름이고 정책상 영어다(카탈로그와 대조해 확인).
- `배치 — , Arrakeen`처럼 카드 이름이 아이콘에 먹히던 버그 소멸. 한 판에서 3회
  나던 것으로, 옛 클라이언트에 새 e2e 단언을 돌려 실패를 확인했다.

### 언어 토글을 미룬 이유

토글 자체는 `TERM_LANGUAGE` 한 줄이지만, **바꿀 영어가 없다.** 실측:

| 대상 | 수 |
| --- | --- |
| 라벨 테이블 행(용어 기반, 이미 ko/en 가능) | 292 |
| 라벨 테이블 **밖**에 하드코딩된 한국어 문자열 | ~261 |
| `index.html`의 한국어 텍스트 노드 | 39 |

즉 진짜 영어 UI는 **약 600개 문자열**을 영어로 뽑아내야 하고, 그중 300개는
테이블이 아니라 코드 안에 박혀 있어 추출 기구부터 필요하다. 지금 토글을 노출하면
용어 몇 개만 바뀌고 나머지는 한국어로 남는 **반대 방향의 혼용**이 된다.

그래서 토글은 별도 결정 사항으로 남긴다. 토대(용어 테이블·`phrase()`·
`phraseText()`·`TERM_LANGUAGE`)는 이미 있으므로 착수하면 기계적인 작업이다.

### 착수 전 근거

- **2a. 용어집.** 공식 KR 룰북 4종이 Dire Wolf 리소스 페이지에 있고 로컬
  `assets/rulebooks/`에도 있다. KR·EN Uprising 룰북은 **둘 다 20쪽이고 페이지가
  정렬돼 있어** 항목마다 `[Main p. N]`을 양쪽으로 인용할 수 있다. 앵커는
  **EN p.20 용어집 페이지**(프로젝트가 이미 `용어집 [Main p. 20]`으로 인용 중).
  산출물은 `docs/rules/glossary-ko.md`와 `scripts/official-rule-sources.json`의
  KR 항목 추가다. **KR FAQ는 존재하지 않는다**(영어만).

  공식 KR URL과 로컬 사본의 SHA-256:

  | 문서 | SHA-256 |
  | --- | --- |
  | `KR_DUNE_IMPERIUM_UPRISING_Rulebook.pdf` | `b9fe3a4c4f9fd4b40c9573a9dc8242e268db35ef1fff753898087b234957b6b6` |
  | `KR_DUNE_IMPERIUM_UPRISING_Rulebook_Supplements.pdf` | `cd763a87485bbf795f556ed03e84f9191b7d7a10e9aedc6ef7b256717996b774` |
  | `KR_DUNE_IMPERIUM_BLOODLINES_Rulebook.pdf` | `d65509007618c81971e652d45e84fb2b36a2eb7ff38525214daaad03aac0fc21` |
  | `KR_DUNE_IMPERIUM_IMMORTALITY_Rulebook.pdf` | `b02ff637ee480d9e58ffc758f30c6bfc02bbfa88bb5d8491bc328be70d7a737b` |

  URL은 `https://d19y2ttatozxjp.cloudfront.net/pdfs/<파일명>`이고 2026-09-20에
  네 건 모두 응답을 확인했다.

- **2b. 아이콘 레이어를 정규식에서 구조적 토큰으로.** 지금 `iconize()`가 렌더된
  산문에 영어 정규식을 돌린다. 대신 서버나 라벨 계층이
  `{term: "trash", count: 1}` 같은 토큰을 내고, 렌더러가 용어집을 보고
  (아이콘, ko 라벨, en 라벨)로 푼다. 이것이 되면 Signet Ring류 충돌이 사라지고
  언어 전환이 가능해지며 용어집이 단일 진실 공급원이 된다.

- **2c. 언어 설정(ko/en)과 완전성 가드.**
  `tests/server/test_action_labels.py`가 이미 파이썬 소스를 스캔해 "모든
  `action_id`에 한국어 라벨이 있는가"를 강제한다. **그 패턴을 prompt와 용어에
  복제한다** — 테이블이 조용히 썩지 않게 하는 유일한 장치다.

  확인해 둔 것(추측이 아니라 검사함):
  - action codec은 prompt와 무관하다(`grep -rl prompt src/dune_imperium/rl/` 0건).
  - 어떤 테스트도 서버가 내보낸 prompt 문자열을 단언하지 않는다.
  - `tests/server/test_snapshot.py`는 최상위 키만 단언하므로 `decision` 안에
    `prompt_ko` 형제를 더해도 깨지지 않는다.

## 3단계 — 레이아웃 (카드 열 토글과 Bene Tleilax board)

**진행 상황 (2026-09-20).** 3a·3b·3c 모두 완료.

| | 상태 | 커밋 |
| --- | --- | --- |
| 3a 카드 열 토글 | 완료 | `6f39e39` |
| 3b Bene Tleilax 크기 | 완료 | `67032b1` |
| 3c 좌석 패널 | 완료 | `7e984ac` |

- **3b**: 열 안의 판은 스캔이 31배 축소(본 보드는 10배)라 연구 칸 20px·좌석 토큰
  10px였다. "크게 보기"가 같은 stage를 1000px로 연다 — **축소 5.6배, 연구 칸
  115px, 좌석 토큰 58px**. `renderBeneTleilaxScan`이 퍼센트로 배치하므로 컨테이너만
  넓히면 끝이고 관리할 두 번째 레이아웃이 없다.
- **3c**: 좌석 넷의 카드 줄(Battle 293px·In play 251·Tech 188·Contracts 147)이
  높이의 대부분이었다. 머리·스탯·존 수치는 남기고 나머지를 "자세히"로 접는다.
  **후반 2,189px → 936px.** 751px 안에 네 좌석이 다 들어오지는 않는다(3.2개쯤) —
  거기까지 가려면 스탯 두 줄(좌석당 93px)을 다시 짜야 하고 그건 별도 작업이다.
  좌석 패널의 마지막 영어 줄(`hand 5 · deck 5 · discard 0`)도 같이 한국어화했다.

- **3a 완료.** 공용 카드 열 여덟 개가 각자의 제목에서 접히고, `c`가 전부
  접었다 편다. 선택은 브라우저에 기억된다. strip을 손으로 만들던 네 자리를
  `stripBox()` 하나로 모았고, 접힘 상태는 DOM이 아니라 Set + localStorage에
  둔다(`#market`은 매 렌더마다 비워지므로 DOM에 두면 남이 한 수 두는 순간
  풀린다).
  **실측한 이득**: 전부 접으면 열이 제 너비를 돌려준다(190px → 170px,
  `#market`의 min-width가 풀리면서). 보드는 그만큼만 커진다(576 → 596px) —
  보드 크기는 센터 그리드가 정하지 지금 이 열들이 정하지 않는다. 처음에
  "보드가 자리를 되찾는다"고 단언했다가 측정해 보고 틀렸음을 확인했다.
- **4단계와 함께 드러난 것**: ≤1100px에서 공용 카드가 보드를 밀어내
  `#board`가 468×0이 되던 결함을 `22b8d69`에서 45% 상한으로 막았다. 그 45%는
  보드를 존재하게 하는 하한이지 숙고한 비율이 아니다 — 3b에서 다시 본다.

- **3a. 공용 카드 열 토글.** Imperium Row · Reserve · Tleilaxu Row를 필요할 때만
  펼친다(단축키 포함). 근거는 위 기준선의 2.2배 넘침이다. 공식 디지털판도
  Imperium Row 접기 단축키를 쓴다.
- **3b. Bene Tleilax board를 strip에서 꺼낸다.** 190px 칼럼의 네 번째 항목이라
  어느 뷰포트에서도 화면 밖이고 33.3배로 줄어 있다. 토글 오버레이로 본 보드급
  크기에 놓는다. (정정) 감사가 "한글 라벨이 달린 fallback 그리드는 도달 불가능한 죽은
  코드"라고 했고 이 문서도 그렇게 적었지만 **틀렸다**. `catalog.py`의
  `"image": versioned(...) if bene_tleilax_image else None`이라서 assets 체크아웃이
  없는 기기에서는 `layout.image`가 없고 그 그리드가 실제로 그려진다. 지우면 안 되는
  폴백이다.
- **3c. 좌석 패널 curation.** 67개 필드에서 "항상 보이는 줄"과 "펼치면 보이는 줄"을
  가른다. 보드 조각 규약(단색 `SEAT_COLORS`, 인쇄된 자리)은 그대로 둔다.

## 4단계 — e2e 그물 넓히기 (3단계의 전제)

지금 `scripts/e2e/`는 **확장을 켠 판도, 끝난 판도, 두 번째 뷰포트도** 보지 않는다
(`common.py:252`가 단일 뷰포트). `disclosure`·`tleilax`·`contract`·`최종 순위`는
검색 결과가 없다. `app.js`를 고치는 작업의 유일한 회귀 검사이므로 3단계 전에
넓혀야 한다.

곁가지로 확인된 종료 화면 결함 둘(별도 슬라이스 후보):

- 종료 배너가 **승자를 말하지 않는다**(`render.js:338`의 "게임이 끝났습니다."뿐).
- `#disclosure` 패널이 전 확장 종료 판에서 1844px·카드 칩 276개로 사이드 칼럼의
  92%를 차지하고, 같은 화면의 리플레이 검토를 스포일한다. e2e 검사는 0건이다.

## 5단계 — 남은 후보 넷 (2026-09-21, WSL 노트북)

사용자 결정(2026-09-21): 핸드오프의 후보 넷을 **전부**, 작은 것부터 — 종료 화면 → 좌석 스탯 →
접근성·온보딩 → 언어 토글. 리플레이 검토 중 disclosure는 **접어 두기**(제목을 눌러 펼침).

| | 상태 | 커밋 |
| --- | --- | --- |
| 5a 종료 화면 둘 | 완료 | `cb99f9c` |
| 5a' 검증 중 드러난 결함 | 완료 — 다섯 건 | `52cb9f8`, `d980ca0`, `b51311a` |
| 5b 좌석 스탯 재설계 | 완료 | `b04032e` |
| 5c 접근성·온보딩 | 완료 | `cd63d39`, `ecbefec` |
| 5d 언어 토글 | 완료 | `92a9ee8`, `09c817d`, `10e737a` |

- **5a.** 종료 배너가 승자와 2위와의 차이를 말한다: 승점 차, 또는 같은 승점이면 처음 갈린 동점 판정
  항목(스파이스 · 솔라리 · 물 · 주둔지 병력 `[Main p. 15]`, 주둔지의 사다우카 지휘관 포함 OQ-047, 마지막으로
  더 늦은 공개 차례 `[FAQ p. 2]`). seed 7 전 확장 판이 실제로 **승점 9 동점을 스파이스 6 대 2로** 가른
  판이라 동점 분기가 실전으로 검사된다. disclosure는 검토 중 접혀 시작하고, 좌석을 바꾸거나 커서를 옮겨도
  보는 사람이 둔 대로 유지되며, 새 검토는 다시 접고, 검토 밖 live 화면은 그대로 전부 보인다. 새 단언
  13개가 옛 클라이언트에서 전부 실패하고, 좌석 전환 유지를 뺀 변이는 정확히 그 단언 하나만 실패시킨다.
- **5a'.** 이 기기에서 한글 스크린샷을 처음 찍자 한 화면에 드러난 것들([`lessons.md`](lessons.md) 2026-09-21):
  1. 검토 상태 줄의 `좌석 0: [object DocumentFragment]` — 용어 토큰 뒤 `describeAction()`이 노드를 돌려준다.
  2. **1700px 이하 모든 화면**에서 `#side`의 두 칸이 반씩 나뉘어 위 칸이 불투명한 로그 패널 밑에 깔렸다
     (1600×1000: 1,734px 내용에 438px 칸). 1366px에서는 최종 순위표가 로그에 가려졌다. 쌓인 행을
     `max-content`로.
  3. 로그의 `Leader Ids: staban_tuek,gurney_halleck` — 엔진은 id 목록을 쉼표 문자열로 낸다.
  4. 재생 중 탐색이 검토 응답이 재생 간격보다 느리면 버려졌다(이 노트북 0.3초 대 0.25초에서 매번).
  5. (리뷰 워크플로가 찾음) 탐색 도중 재생을 누르거나 간격을 바꿔도 같은 식으로 버려졌다. 탐색이 날아가는
     동안은 재생 틱이 요청을 내지 않고, 탐색이 그린 뒤 다음 수를 예약한다.

  각 결함마다 옛 코드에서 실패하는 e2e 단언을 더했다(경합 둘은 `page.route`로 응답을 0.6초 늦춰 기기
  속도와 무관하게). `spectate.py`의 끝 단계는 이 노트북에서 옛 코드로도 매번 시간 초과였는데 4번 수정으로
  통과한다. 12종 전부 통과(`d980ca0`).

- **5b.** 좌석 넷이 1600×1000의 751px 칸에 **다 들어온다**. 높이를 키운 것은 스탯 자체가 아니라 줄바꿈이었다:
  긴 리더 이름의 두 줄(넷 중 셋, +23px), 병력 줄 끝 `Commander 0/1` 텍스트(+21px), 두 줄짜리 존 줄
  (+17px), 전투 중 영어 `Conflict` 칩. 이름은 한 줄 말줄임(전체는 hover 카드), 책략 카드 수는 자원 줄로
  (인쇄 아이콘이 있다), 주둔지 지휘관은 보드와 같은 `C2` 칩, 교전 칩은 용어 "교전", 존 줄은 핸드·카드덱·
  버림 더미만 — 개인 공급처(12 − 주둔지 − 교전으로 유도됨)와 공급처 지휘관은 "자세히" 안과 주둔지 title로.
  스탯 라벨도 영어 대신 용어집 단어. seed 7 전 확장 판 25/50/75/95% 실측: 1600×1000·1920×1080은 모든
  시점에 들어오고(좌석 207~258 → 169~193px, 내용 986 → 약 720px), 1366×768은 칸이 519~652px라 여전히
  스크롤한다(내용 873~986 → 703~731px). 썸네일 옆에 스탯을 붙이는 배치안은 CSS 주입으로 재 보니 스탯이
  좁아져 더 줄바꿈되어 버렸다(V1~V4, 이득 0~40px).
- **5c.** 화면에 안 보이는 `aria-live` 영역이 결정 좌석의 변화와 게임 종료(승자)를 읽는다. 한 화면이 사람
  좌석 여럿을 맡는 open 서버를 위해 "당신 차례"에도 좌석을 붙인다. 오류 줄은 `role="alert"`, 연결 알림은
  `status`, 검토 막대의 ⏮ ◀ ▶ ⏭와 슬라이더·좌석 토큰에 이름. 도움말 창(머리글 버튼 또는 `?`, 한글
  자판에서도 `Slash` 키로)은 TERMS에서 바로 만든 아이콘 범례(한국어 + 영어), 인쇄 아이콘이 없는 좌석
  표시(`C2`, `1st` = 용어집의 시작 플레이어 마커 `[Main p. 14]`, 교전 칩), 단축키 넷, UI 단계 그대로의 한
  턴 진행. `help.py`(13번째 e2e)는 범례를 렌더러 재실행이 아니라 **좌석·손패에 실제로 그려진 아이콘이
  전부 범례에 있는가**로 교차 확인한다. 리뷰 워크플로가 찾은 것: 앞 좌석이 되돌릴 수 있는 행동으로 턴을
  마치면 요약에 확정 대기와 다음 좌석이 함께 오는데 알림이 다음 좌석의 차례를 **미리** 말하고 실제
  넘김은 말하지 않았다(`ecbefec`). 첫 `help.py` 단언은 그 조기 알림을 통과시켰다 — 이제 `announce()`를
  감싸 실제로 말한 순서를 본다.

- **5d.** 머리글의 **English / 한국어** 버튼이 화면 언어를 바꾸고 브라우저에 기억된다(기본 한국어). 한 판의
  어떤 화면에서든 영어로 바꾸면 **한글이 하나도 남지 않고**(버튼 자신만 "한국어"), 다시 누르면 같은 화면이
  바이트 단위로 돌아온다 — `lang.py`가 live 테이블(몇 수 둔 뒤 포함)·도움말·끝난 판의 검토와 순위표·종료
  배너·설정 화면에서 텍스트·title·alt·aria-label 전부를 훑는다. 구조:
  - `TERM_LANGUAGE` 하나가 스위치다. 라벨 테이블은 제자리에서 교체한다(`LABELS_EN`, 10개 테이블 589행) —
    `ACTION_LABELS[id]` 호출부 약 40곳은 그대로다.
  - 코드 안 문자열은 `UI_TEXT`(323항목)와 `t()`/`tNode()`로. `{{구멍}}`은 용어를 펼친 **뒤** 채우므로
    사람 이름(남이 입력한 값)이 용어나 마크업으로 읽히지 않는다. 구멍 뒤 조사(을/를·이/가·으로/로)는
    값에 따라 달라지므로 문장을 고쳐 피했다.
  - 엔진 prompt(`rules/*.py`, 사람에게 보이는 65개)는 영어로 오므로 `PROMPT_KO`(63) + 패턴(3)으로 옮긴다.
    한국어 모드에서 prompt가 **처음으로 한국어**가 됐다(전에는 엔진 영어 그대로였다).
  - `index.html`은 `data-i18n` 표시, 로그 payload 키는 `PAYLOAD_KEY_LABELS`(95종; 전에는 `Card Id`·`Space Id`
    같은 영어 키 89종). `source` 값은 조각 중 카탈로그 이름만 남기고(`imperium:high_priority_travel:1` →
    High Priority Travel) 나머지 부기 값은 뺀다. supply 부족 경고는 서버가 한국어 문장과 함께 구조화된
    `shortfall`을 보내 클라이언트가 언어에 맞게 쓴다.
  - 한국어 테이블에 남아 있던 영어를 용어집 단어로: 단계(게임 준비 `[Main pp. 3-4]`, 플레이어 차례·전투·
    메이커스·소환 `[Main p. 8]`, 종료 단계), 팩션, Agent 아이콘, 공용 덱(임페리움·책략·교전 카드덱 `[Main
    p. 4]`, 용어집에 추가). 테이블 밖 하드코딩 한국어 **273 → 0**.
  - 가드 `tests/server/test_i18n.py` 7개: 테이블 밖 한글 리터럴 0, 모든 키가 두 언어로 있고 쓰이며 구멍이
    같음, `LABELS_EN`이 한국어 테이블과 키·용어 토큰이 같음, HTML 인라인 한국어 = 표의 ko, 사람에게 보이는
    모든 prompt(조건식 가지 포함)에 한국어.

  **만든 방법과 검증.** 기반(`92a9ee8`)을 먼저 쓰고, 파일별 변환·영어 쌍둥이·한국어 prompt·payload 키를
  13 에이전트 워크플로로 병렬 처리한 뒤 병합했다. 이어 36 에이전트 검증 워크플로: 코드 결함 22건 중 18건
  확인 → 전부 수정(로그에서 **좌석 0이 사라짐**, 재생 중 언어 전환이 **재생을 멈춤**, 검토 좌석 선택지가 옛
  언어로 남음, 카드 **비용**이 "설득 비용"으로 바뀌어 Persuasion과 구분이 사라짐, 좌석 상태 줄의 아이콘
  소실 등), 한국어 prompt 21건(용어 `{intrigue}`가 이미 "책략 카드"라 **"책략 카드 카드"**, recall을 용어집
  "소환" 대신 "회수", 조건식 prompt 4개를 가드가 놓침), 영어 57건("Intrigue card card", "[아이콘]ed").
  같은 중복이 한국어 테이블에도 22곳 있었다(지난 세션의 토큰화가 만든 것; 아이콘으로 그릴 때는 가려진다).
  `lang.py`는 셋을 더 잡았다: 알림 영역이 전환 전 문장을 들고 있음, 검토 좌석 선택지, 숨은 설정 화면의 좌석
  선택 — 그리고 빠른 왕복 전환 때 취소되지 않은 알림 타이머가 옛 언어 문장을 나중에 덮어쓰는 경합.

  **한국어 모드의 영어 잔재(같은 날 다음 단위).** 검증이 찾은 27건과 스크린샷에서 본 몇 곳을 용어집
  단어로 옮겼다: 공용 카드 열 제목(임페리움 열·예비 카드·틀레이락스 열·베네 틀레이락스 게임판 — 보이는
  제목만 번역하고 접힘 키 `data-strip`은 지금의 영어 이름으로 고정해 저장된 접힘과 e2e가 그대로 산다),
  메이커 작살·은행 `[Main p. 20]`·보너스 스파이스(한국어 룰북 15쪽)·전투 장소 `[Main p. 10]`·틀레이락스 트랙
  `[Immortality p. 16]`·뒤집기 `[Bloodlines p. 12]`·플레이 영역 `[Main p. 20]`·초암 모듈, 순위표 머리글
  (승점·스파이스·솔라리·물·주둔지 병력), 좌석 세부 줄 이름, `YOU`→나·`1st`→시작(title은 시작 플레이어
  마커), 로비의 `AI` 배지→좌석 종류, 검토 상태의 `step`→수, 한국어 테이블에 영어 문장으로 남아 있던 네 행.
  `lang.py`에 **한국어 모드에서 이름이 없는 크롬**(머리글·검토 상태·열 제목·순위표 머리글·스탯 title·좌석
  줄 이름)에 `TERMS`의 영어 낱말이 없는지 보는 단언을 더했다(직전 커밋에서 열 제목으로 실패). 그 과정에서
  나온 두 가지: 검토 중 전환이 새 검토 상태를 받은 **뒤에야** 다시 그렸고, 숨은 게임 목록은 설정 화면으로
  돌아가 다시 받을 때까지 옛 언어였다.

**검증 메모.** 13종 e2e 전부 통과(`cd63d39`). 이 노트북에서 본 흔들림 둘: `open_mode.py`의 폴링 따라잡기
4.5초는 무거운 작업을 곁에 돌리면 넘는다(4.58초), `remote.py`는 원격 서버가 seed를 받지 않아 [11]에서
되돌릴 수가 없는 판이면 검사 둘을 건너뛴다(56 → 54, 실패 아님). `spectate.py`의 재생 간격 검사는 그려진
시각 대신 요청 시각을 재도록 고쳤다(`081bbdb`).

**5d로 넘긴 실측**(전 확장 3판의 끝난 로그): payload 키가 영어 그대로 **89종**(`Card Id` 904회,
`Space Id` 860, `Count` 569, `Effect` 395 …)이고, 값에도 엔진 문자열이 샌다(`Source:
imperium:high_priority_travel:1`, `Effect: may_recall_spy_for_three_strength`, `Removed:
imperium:rebel_supplier:1,…`, `To Space: paid_trash`). 결정 prompt는 엔진의 영어 문장이 그대로 나온다
(`rules/*.py`의 `prompt=` 55곳). 라벨 테이블 행 489개도 용어 토큰 밖의 한국어(`"{leader} 선택"`의
"선택")가 있어 영어판이 따로 필요하다 — "292행은 이미 ko/en이 된다"는 앞 기록은 토큰만 본 것이다.
테이블 밖 하드코딩 한국어는 249개, `index.html`은 44개.

## 6단계 — 남은 후보 셋 (2026-09-21 밤, Mac mini)

사용자 결정(2026-09-21 밤): 핸드오프 후보 중 **셋**을 작은 것부터 — `remote.py` 테스트 위생 → 로그의 영어
조각 → 1366×768 좌석. 한글 카드 스캔은 고르지 않았다. master 체크아웃에서 학습(long-2k)이 돌고 있어
워크트리 `ui-leftovers`에서 했다.

| | 상태 | 커밋 |
| --- | --- | --- |
| 6a `remote.py` [11]의 무작위 건너뜀 | 완료 | `f97f838` |
| 6b 로그·행동 목록의 엔진 id | 완료 | `80d483e` |
| 6c 노트북에서 좌석 넷 | 완료 | `4a9a8b5` |
| 6d 보드 위 사다우카 지휘관(사용자 요청) | 완료 | `3fa7b82` |
| 6e 한 관측소의 Spy를 쌓기(사용자 지적) | 완료 | `44b5591` |

- **6a.** 원격 서버는 seed를 받지 않아 [11] 시점에 되돌릴 창이 열린 좌석이 없으면 되돌리기 검사 둘을
  건너뛰었다(56 → 54). 이제 창이 열린 좌석이 나올 때까지 한 수씩(최대 30) 더 두고, 끝내 없으면 실패한다.
  네 번 돌려 네 번 57개 통과, 그중 한 번은 두 수를 더 두고서야 되돌릴 수 있었다(예전이면 건너뜀).
- **6b.** "작음"으로 적혀 있었지만 재 보니 컸다. 끝난 AI 판 19개(모든 확장 조합, heuristic·random 좌석)의 로그
  전체를 페이지 자신의 함수로 두 언어에 걸쳐 그리니 엔진 문자열이 **277곳**에 샜다: 무작위 결과 줄
  `chance: Round:2:Player:0:Discard Shuffle`, 행동 머리줄의 `Count: 2`·`Option: 0`·관측소 id(`Arrakis Hagga
  Basin`)·연구 좌표(`C2r2`)·진영(`Bene Gesserit`), payload의 `Skill:Desperate:0`·Feyd 트랙(`mid_trash`)·
  Family Atomics가 치운 카드의 쉼표 목록, 라벨 없는 행동 6·이벤트 1. 영어 모드의 한글은 0이었다.
  - 값 해석기 하나(`fieldText`, `core.js`)를 행동 목록·로그·검토가 같이 쓴다. 관측소는 **지켜보는 칸 이름**
    (`관측소 (Hagga Basin)` — 카탈로그에 `post_spaces`를 더함; 관측소에는 인쇄된 이름이 없다), 연구 칸은
    `연구 트랙 2-2 (스파이스 1)`(좌표는 전사 자료의 것, 용어집 "연구 트랙" `[Immortality p. 16]`), Feyd
    트랙은 칸의 **인쇄 보상**(`FEYD_TRACK_LABELS`; 시작은 "맨 왼쪽 칸" `[Main p. 17]`), 스킬 인스턴스와 쉼표
    목록은 카탈로그 이름, 진영·영역·보상·선택은 `TERMS`와 `VALUE_LABELS`. 무작위 결과는 id의 모양을 읽어
    "좌석 1의 버림 더미 섞기 (Arrakeen)" / "좌석 1, 좌석 2에게서 책략 훔치기"로 쓴다.
  - 같은 id가 보드에도 샜다: 관측소 title(`prettify(postId)`), 연구 칸 title(`c2r2`), 좌석 세부의 연구
    플래그(`c5r1`), Agent turn의 스파이 소환 줄. 모두 같은 이름을 쓴다.
  - 한국어 표의 영어 네 행: `{discard} reshuffle`(이벤트), Shield Wall 지불 행동의 `2 spice … sandworm`,
    `Research 시작`, 스파이 소환을 "회수"라 쓴 줄(용어집은 스파이 소환 `[Main p. 20]`; 회수는 병력 withdraw).
  - 라벨 가드가 리터럴만 훑어 **행동 6개·이벤트 1개**를 놓쳤다([`lessons.md`](lessons.md) 2026-09-21).
    이제 `ACTION_HANDLERS`(엔진 디스패치 표)와 괄호 없는 `kind="a" if … else "b"`까지 본다. 새 표를
    `LABEL_TABLES`에 등록하지 않으면 언어 전환이 그 표를 바꾸지 않으므로 그것도 가드한다.
  - 다시 잰 19판: `prettify` 호출 0, id 모양 0, 한국어의 라틴 낱말은 이름과 용어집이 일부러 영어로 둔
    구뿐. `scripts/e2e/log_words.py`(15번째 E2E)가 이를 지킨다 — 옛 클라이언트에서 6개 실패, 지금 13개 통과.
    반박 검증 워크플로(코드·문구·규칙 의미 세 관점, 확인자 1)는 확인된 결함 0.
- **6c.** 1366×768에서 좌석 칸은 **519~652px**를 오간다(손패 줄이 두 줄이 되면 133px를 가져간다). 접힌
  좌석 넷은 어느 시점이든 703~738px였고, 1440×900·1536×864도 스크롤했다. CSS 주입으로 먼저 쟀다:
  지도자 그림을 44px로 줄이거나 없애도 똑같이 156px(머리 높이는 그림이 아니라 이름·배지가 정한다),
  "자세히"를 존 줄과 나누면 존 줄이 두 줄로 접혀 이득 0, 작은 그림을 이름 앞에 두면 이름 줄이 두 줄이 돼
  141px. 채택: **높이 960px 이하**에서 그림을 빼고(이름의 hover 카드는 그대로) 이름·배지를 한 줄로, 접기를
  머리 줄 끝의 화살표로(단추 글자는 접근 이름과 title로 남는다). 좌석 171 → **111px**, 세 해상도의 네
  시점 전부 들어온다. 1600×1000 이상은 이미 들어오므로 그림을 유지한다. `seats.py`에 노트북 검사(21 → 31;
  옛 클라이언트에서 5개 실패).

- **6d.** 사용자 요청: "bloodlines 룰북 3p에 나온 보드판 사진처럼 사다우카 지휘관이 위치한 보드 에이전트 칸의
  오른쪽 위에 … 룰북에 있는 지휘관 이미지가 들어가면 좋겠어." setup은 지휘관을 다섯 칸(4인은 Assembly Hall까지
  여섯)에 두며 "Agent를 놓을 자리는 남긴다" `[Bloodlines p. 3]`. 보드는 테두리 안에 빨간 "C" 배지를 그리고
  있었다. 그림은 룰북 2쪽 부품 목록의 모형이다 — PDF 안에서 모형 두 조각(JPEG)과 흐린 그림자가 따로 들었고
  조각에는 베이지 바탕과 그림자 후광이 박혀 있다. 후광은 바탕과 색상이 같고 명도만 낮으므로 "바탕색을 어둡게 한
  선"에서 벗어난 정도(색상 잔차)로 모형만 남기고, 바탕과 같은 베이지인 칼날은 능선으로 찾아 살렸다(색 거리만
  쓰면 후광이 남고, GrabCut은 주먹을 잘라 냈다). `scripts/cut_commander_token.py`가 고정된 공식 PDF에서
  바이트 단위로 같은 그림을 다시 만든다; 그림은 에셋 저장소 `tokens/`(`1574241`). 자리는 사진의 다섯 칸에서
  읽은 평균 — 받침 중심이 테두리 폭의 0.89·높이의 0.13, 키는 테두리의 0.95배(사진에서 0.84~0.94, 0.04~0.26,
  0.83~1.1배). 칸 클릭은 가로채지 않고, 칸 이름이 "사다우카 지휘관 (2 솔라리)"를 말한다. `board_tokens.py`
  187 → 200(옛 클라이언트에서 8개 실패).
- **6e.** 사용자 지적: "spy도 원래 한 칸에 여러개 놓이는 경우는 기존에 있던 스파이의 위에 쌓는 식인데 지금은 옆으로
  쭉 나열되게 구현됐어." 관측소는 원래 빈 곳에만 놓고, 상대 Spy와 함께 쓰는 길은 둘이다 — Double Agent("may share a post
  with an opponent's Spy", `uprising-systems.md`)와 Bloodlines의 잠복 스파이("놓을 때 상대의 Spy를 무시할 수 있다.
  자신의 Spy가 이미 있는 post에는 놓을 수 없다" `[Bloodlines pp. 5, 12]`, `bloodlines.md`; Arrakis Observer·Spy
  Drones·Deliver Supplies 계약 토큰). 둘 다 자기 Spy끼리는 겹치지 않으므로 한 관측소에 많아야 좌석 수만큼 선다. 규칙 문서는 모양을 정하지 않으므로 표시 관례로 다뤘다. 모든 Spy를 원판
  너비로 두고, 나중 Spy가 아래 Spy의 윗면에 선다 — 원통의 두 면 중심이 아이콘 80 중 48 떨어져 있어 한 층이 Spy
  높이의 60%. 엔진은 좌석마다 관측소만 들고 누가 먼저 왔는지는 없으므로, 로그의 `spy_placed` 순서(되돌린 수 제외,
  검토는 커서까지)로 쌓고 로그가 모르는 Spy는 좌석 순으로 아래에 둔다. `board_tokens.py` 200 → 203(옛 나란히
  배치에서 5개 실패).

**남은 것** — 이번 범위 밖에서 본 것:
1. **패널·보드 문구의 용어집 잔재 10곳.** 용어집에 한국어가 있는데 영어로 남은 크롬: `Hand (공개)`(핸드),
   `Intrigue 덱 맨 위`(책략 카드덱), `Intrigue 해결 중`, `내 discard`(버림 더미), `Navigation N장 남음`(운항
   카드), `Combat Intrigue 창`(전투 책략 카드), `(Flip됨)`(뒤집기), `Tactics N칸`(전술 트랙), `Ixian
   Embassy`(익스 대사관 판), 설정 화면의 확장 이름(혈통·불멸·봉기, 용어집 "적용"). (`보드에 남은 Commander
   없음`은 6d에서 고쳤다.) `lang.py`의 이름 없는 크롬 검사가 이 자리들을 보지 않는다.
2. **용어집에 행이 없는 로그 낱말.** Gather Intelligence, Infiltrate, Other Memories, Secret Project,
   set-aside, Wild card, Family Atomics, Immediate, Usurp — 한국어 룰북에서 인용을 찾아 용어집에 더한 뒤에만
   바꾼다(용어집 규칙 2). `log_words.py`의 `KOREAN_KEEPS_ENGLISH`가 지금 이 목록이다.

## 감사에서 기각된 것 (다시 꺼내지 않기 위해)

61건 중 29건이 반박 검증에서 기각됐다. 되풀이하기 쉬운 것만 적는다.

- **AI "생각 중" 표시**: M14 실전 판은 친구 4명이라 AI 좌석이 없다. 그 대기는
  일어나지 않는다.
- **용어 일괄 통일 스윕**: 위험하다. trash/discard/draw/recruit는 현재 100%
  영어이고, 넘겨짚은 번역이 실제 버그를 만든 기록이 [`lessons.md`](lessons.md)에
  있다. 용어집 없이 손대지 않는다.
- **모바일 레이아웃**: [`multiplayer-design.md`](multiplayer-design.md)가 명시한
  비목표다. 다만 600~900px(친구가 Discord 옆에 반쪽 창으로 띄우는 폭)은 실제
  사례라 3단계에서 함께 본다.
- **창 크기 변경 대응**: 이미 안전하다. 보드 조각이 전부 `.board-stage` 기준
  퍼센트라 resize 리스너가 필요 없다.
- **차례 알림**: 이미 구현돼 있고 설계와 맞는다(탭 제목 + WebAudio 880Hz,
  `isRemote()` 게이트). 평문 HTTP에서 Notification API를 쓰지 않는 것은 설계 결정이다.

## 작업 규약

- 학습이 도는 동안 이 체크아웃의 `src/`를 고치지 않는다(spawn worker와 지연
  import가 작업 트리를 읽는다 — [`lessons.md`](lessons.md) 2026-09-16).
  **UI 작업은 워크트리에서 한다.** 문서와 `scripts/`는 괜찮다.
- `app.js`를 고쳤으면 pytest로는 부족하다. [`scripts/e2e/`](../scripts/e2e/README.md)를
  돌린다(스크래치 Playwright venv + 시스템 Chrome).
- 규칙 동작을 바꾸는 항목은 이 계획에 없다. 이 계획이 규칙 공백에 닿으면
  [`rules/open-questions.md`](rules/open-questions.md)에 먼저 적는다.
