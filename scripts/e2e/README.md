# 브라우저 E2E (`scripts/e2e/`)

플레이 서버의 브라우저 UI(`src/dune_imperium/server/static/`)를 실제 Chrome으로 검증하는 스크립트다.
`pytest`는 JavaScript를 실행하지 않으므로, `static/*.js`를 고친 뒤에는 이 스크립트들이 유일한 회귀 검사다.
2026-09-17 세션에서 스크래치로만 두었다가 다음 세션(다른 기기)에서 전부 다시 만들어야 했기 때문에
저장소에 둔다. **Playwright는 프로젝트 의존성이 아니다**(설계 문서 10절) — 저장소 밖 스크래치 환경으로 돌린다.

## 준비 (기기마다 한 번)

```bash
uv venv /tmp/dune-e2e-venv --python 3.12
uv pip install --python /tmp/dune-e2e-venv/bin/python playwright
```

브라우저는 시스템 Chrome(`channel="chrome"`)을 쓴다. Chrome이 없는 기기(WSL 등)는 실행 파일을 지정한다:
`E2E_CHROMIUM=~/.cache/ms-playwright/chromium_headless_shell-*/chrome-linux/headless_shell`
(WSL에서는 버전 심볼을 갖춘 `libasound.so.2` stub을 `LD_LIBRARY_PATH`에 둬야 했다 — handoff 2026-09-17).
서버는 이 체크아웃의 `.venv/bin/dune-imperium-server`를 빈 포트에 직접 띄우고 끝나면 내린다(`common.ServerProcess`; `kill()`은 SIGKILL이다)
(`uv sync --extra ui`가 돼 있어야 한다). 다른 체크아웃을 검사하려면 `E2E_REPO=<경로>`.

## 실행

```bash
cd scripts/e2e
/tmp/dune-e2e-venv/bin/python remote.py        # 약 25초; `remote.py 120`이면 첫 구간을 120 스텝으로
/tmp/dune-e2e-venv/bin/python open_mode.py     # 약 40초; `open_mode.py game` / `open_mode.py bell`로 절반만
/tmp/dune-e2e-venv/bin/python races.py --ab    # 약 40초
/tmp/dune-e2e-venv/bin/python recovery.py      # 약 30초
/tmp/dune-e2e-venv/bin/python turn_controls.py # 약 20초; 병력 수 조절기와 Reveal 구매 패널
/tmp/dune-e2e-venv/bin/python turn_end.py      # 약 20초; 턴 종료 컨트롤 하나로 통일(리더 드래프트 마지막/비마지막 뽑기, finish_agent_turn·finish_reveal·pass_combat_intrigue·pass_endgame_intrigue, 되돌릴 수 없는 종료, 영어 표기)
/tmp/dune-e2e-venv/bin/python staged_turn.py   # 1~2분; Agent turn의 단계별 선택(카드 → 칸 → 남은 선택, graft는 두 장 먼저)
/tmp/dune-e2e-venv/bin/python board_tokens.py  # 약 15초; 인쇄된 자리에 놓이는 조각(칸 hotspot의 흰 테두리·Agent·Spy 말·Control·bonus spice·Maker Hooks·Alliance·사다우카 지휘관)과 Reveal 미리보기·Intrigue 더미
/tmp/dune-e2e-venv/bin/python leader_card.py   # 약 60초; 좌석 리더 팝오버에 그려지는 좌석 자신의 상태(Feyd 훈련 트랙·Chani 전술 트랙 토큰, Y'rkoon의 Navigation 카드 네 칸, Kota의 Secret Project 타일, Shaddam의 따로 치워둔 사다우카 계약 두 장, 리뷰 라벨의 폴백, Jessica의 뒤집힌 그림), 좌석 상태 줄이 그 세 플래그를 더는 되풀이하지 않는지, 두 언어 + --remote 두 컨텍스트 + 실제 판 리뷰 재현
/tmp/dune-e2e-venv/bin/python spectate.py      # 약 35초; 전 좌석 AI 게임의 관전(리플레이 검토의 자동 재생)
/tmp/dune-e2e-venv/bin/python endgame.py       # 약 40초; 끝난 판의 최종 순위·종료 후 공개, 노트북 1366x768
/tmp/dune-e2e-venv/bin/python narrow.py        # 약 45초; 1100px 미만 레이아웃(전 확장) + 좁은 창의 머리글(600~1366px, 두 언어)
/tmp/dune-e2e-venv/bin/python columns.py       # 약 40초; 공용 카드 열 접기와 Bene Tleilax 크게 보기
/tmp/dune-e2e-venv/bin/python seats.py         # 약 35초; 좌석 패널의 자세히 접기, 노트북 1366x768에서 좌석 넷
/tmp/dune-e2e-venv/bin/python help.py          # 약 30초; 도움말 창과 스크린 리더 알림(aria-live·role·이름)
/tmp/dune-e2e-venv/bin/python lang.py          # 약 1분; 한국어/English 전환 — 영어에서 한글 0, 왕복 동일, 새로고침 유지, 카드 이름·그림이 언어를 따름
/tmp/dune-e2e-venv/bin/python log_follow.py    # 약 10초; 실시간 행동 로그가 게임을 계속 따라가는지(전 확장 + 지도자 드래프트, 3라운드+)
/tmp/dune-e2e-venv/bin/python log_words.py     # 약 10초; 행동 로그·행동 목록·보드 title에 엔진 id가 없는지(두 언어)
/tmp/dune-e2e-venv/bin/python log_passes.py    # 약 10초; 이어진 Combat/Endgame Intrigue 패스가 카드 하나로 접히는지(기본판+전 확장, 두 언어)
/tmp/dune-e2e-venv/bin/python remote_fresh.py  # 약 15초; 원격 판에서 로그가 내 마지막 수 이후 전부(상대 셋 + 그 사이 중립 카드) 빛나는지, 내 카드 자신은 아닌지
/tmp/dune-e2e-venv/bin/python combat_result.py # 약 10초; 전투 해결 뒤 배너에 뜨는 결과 줄(승자·순위·내 보상), 기본판+전 확장, 두 언어
/tmp/dune-e2e-venv/bin/python intrigue_options.py # 약 15초; 두 선택지를 가진 Intrigue 카드 한 장이 그 카드 이름 뒤에 "선택지: 0/1"이 아니라 카탈로그의 실제 문구를 보이는지, 두 언어
/tmp/dune-e2e-venv/bin/python trash_zone.py    # 약 10초; 같은 이름의 트래시 후보 둘이 보는 좌석의 다른 보관 구역(핸드/버림 더미/플레이 영역)에 있을 때 행이 그 구역을 이름으로 구별하는지, 두 언어
/tmp/dune-e2e-venv/bin/python save_delete.py   # 약 10초; 저장 목록의 삭제 버튼 두 번 누르기 확인(ITEM 8d), 두 언어
/tmp/dune-e2e-venv/bin/python refresh_error.py # 약 5초; 새로고침 실패 배너가 뒤늦게 성공한 새로고침에 스스로 지워지는지(ITEM 8g), 내 행동이 낸 오류는 지워지지 않는지
E2E_HOST=100.x.y.z /tmp/dune-e2e-venv/bin/python rehearsal.py   # 약 1분; 실제 원격 판 전의 리허설
```

종료 코드 0이 통과다. 실패하면 뒤처진 페이지의 상태와 요청·콘솔 타임라인을 출력하고, 서버 로그 사본을
`$TMPDIR/dune-e2e-server-last.log`에 남긴다.

| 스크립트 | 무엇을 보나 |
|---|---|
| `remote.py` | `--remote` 서버, 쿠키가 분리된 컨텍스트 둘(호스트·친구). 설계 10절 시나리오: `#admin=` 진입 → 방 생성 → 방 링크 → 좌석 고르기·claim → 교차 좌석 403 → 이름(마크업 주입 시도 포함)·접속 점 → **서버가 지목하는 좌석이 한 스텝씩 두고 매 스텝 뒤 두 페이지가 서버 상태로 수렴하는지**(revision·confirmation·players 일치, 둘 차례인 페이지는 그 revision의 actions 보유, 아닌 페이지는 actions 없음) → 대기 배너·탭 제목 → 새로고침 복귀 → 호스트의 release → 재claim → 되돌리기 → 자리 비우기 → claim 도중 나가기·"이어 하기" → 실패한 요청·JS 예외·서버 오류 0. 원격 서버의 새 게임 화면은 좌석 넷이 모두 사람으로 시작하는지(2026-09-24; 전에는 좌석 1~3이 AI라 친구가 링크를 열면 빈 좌석이 없었다). |
| `open_mode.py` | 기본(open) 서버의 회귀. (A) 한 화면이 사람 2 + heuristic 2를 끝까지: 스텝마다 POST 1 + snapshot 1, 매 스텝 클라이언트 로그 길이 = 서버 `log_count`, 되돌리기의 epoch 변경, 이어 붙인 로그 == 서버 전체 로그, 순위표. (B) 검토 모드에서 늦게 온 옛 응답이 화면을 덮지 않는다. (C) 컨텍스트 셋: 초인종 반영 1초 안·snapshot 1개·자기 행동에는 추가 요청 0, 스트림을 막은 컨텍스트의 폴링 전환, 게임 삭제 통지. (D) open 서버의 새 게임 화면은 그대로 사람 1 + heuristic 3으로 시작하는지(2026-09-24). |
| `races.py` | 응답 순서를 강제로 뒤집는 개입 실험. (1) 기다리는 페이지의 좌석 snapshot을 0.7초 붙잡은 사이 상대가 두 번 더 바꾼다 → 낡은 응답을 채택한 뒤 single-flight의 다음 바퀴가 따라잡아야 한다. (2) 새로고침한 페이지의 스트림을 0.3초, 입장 snapshot을 0.9초 늦춘다(그 snapshot은 `online: false`로 읽혔다) → 비행 중에 온 players 초인종이 한 바퀴를 더 예약해야 한다. `--ab`는 그 예약을 끈 `session.js`로 먼저 돌려 **실제로 stale이 되는지**(검사가 실패할 수 있는지) 확인한다. |
| `recovery.py` | 자동 저장과 복구(슬라이스 5). 원격 한 판을 24 스텝 둔 뒤 서버를 **SIGKILL**하고 같은 포트·같은 저장 폴더로 다시 띄운다: 게임당 파일 하나·`.tmp` 잔재 없음·자동 저장이 live 상태보다 한 턴 이상 뒤처지지 않음, 호스트 패널의 저장 목록, 기다리던 친구의 "서버 연결 끊김" 표시 → 서버가 돌아오면 "새 방 링크로 들어오세요" landing(죽은 방의 "이어 하기"는 제안하지 않음), 호스트가 관리자 링크 → 저장 목록 → 불러오기(새 game id, revision = 자동 저장의 step 수, 좌석 전부 빔), 둘 다 새 링크로 복귀해 16 스텝 수렴, 불러온 게임은 자기 슬롯에 자동 저장. |
| `staged_turn.py` | 사람용 Agent turn UI(2026-09-19). 서버는 Agent turn을 `agent_turn` 행동의 평평한 목록으로 주고(코덱·학습용), 페이지는 그것을 **카드 → 보낼 칸 → 남은 선택** 단계로 보여 주다가 남은 행동 번호 하나를 POST한다. 매 단계에서 화면을 그 목록과 대조한다: 빛나는 카드·칸이 목록이 허용하는 것과 정확히 같은지, 고르는 동안은 요청이 없는지, Escape·단계 칩·다른 카드로 취소·교체가 되는지, 칸을 먼저 골라도 되는지, 다 고르면 **그 행동 하나가** 요청 한 번으로 실행되는지, 남은 placement가 여럿이면(비용 옵션·graft) 선택창과 패널이 정확히 그것들을 내놓는지, "전체 행동 목록 보기" 토글이 모든 합법 행동을 나열하고 새로고침 뒤에도 유지되는지. 선택지가 여럿인 (카드, 칸)은 고정 seed 판에서 찾고 없으면 건너뛴다. 마지막으로 Immortality 판에서 **graft는 두 장을 먼저** 고른다: 함께 낼 수 있는 카드의 ＋ 표시, 두 번째 카드의 합류(교체가 아니라), 빛나는 칸이 두 카드의 graft placement의 합집합인지, 칸을 누르면 placement와 파트너 선택이 요청 둘로 이어 실행돼 두 카드가 in play가 되는지(graft placement가 여섯 판 안에 안 나오면 건너뛴다; 약 1~2분). |
| `turn_controls.py` | 병력 수 조절기와 Reveal 구매 패널(2026-09-19). `deploy_troops` 1·2·…가 패널에서 **한 줄**로 접히고 같은 줄이 보드의 Conflict 구역(자기 사분면 옆)에도 서는지, 보드에서 숫자를 바꾸면 패널 숫자도 바뀌고 요청은 없는지, 확정 버튼의 "지금 → 뒤" 미리보기가 서버의 `strength_after`와 같고 **실행 뒤 실제 전투력과도 같은지**, 회수(`withdraw_troops`)가 count 하나여도 같은 조절기로 되는지. Reveal에서는 `summary.decision.persuasion`과 패널의 표시, 살 수 있는 카드마다의 비용, `finish_reveal`이 패널에는 없고(턴 종료 컨트롤 하나로 통일, turn_end.py) 배너의 턴 종료 줄로만 나가는지, 사는 카드가 남아 있는 동안(구매 전부터) 그 줄에 "구매 끝"이 붙고 구매 뒤에도 남은 게 있는지에 따라 그대로 따라가는지, 테이블의 빛나는 카드를 누르면 그 비용만큼 Persuasion이 줄고 "산 카드"에 오르는지, 누르면 이 좌석이 다시 붙잡히지 않고 한 번에 넘어가는지. |
| `turn_end.py` | 턴 종료 컨트롤 하나로 통일(2026-09-24, 사용자 지적 — 리더를 마지막에 고를 때 턴 종료를 못 누르고 바로 다음으로 넘어가거나, `Agent 턴 종료`를 누르면 확정 단계가 한 번 더 뜨는 등 일관성이 없었다). 배너의 `div.confirm-row.turn-end-row` 한 줄, 버튼 문구는 항상 "…턴 종료 ▶"로 끝난다: (a) 리더 드래프트에서 사람 좌석이 **마지막으로**(선공이라서) 고르면 고른 뒤 그 줄이 뜨고 `#actions`는 비어 있으며, 누르면 `/confirm`이 정확히 한 번 가고 그다음 그 좌석의 1라운드 행동이 뜨는지; (b) 마지막이 아닌 뽑기도 같은 줄이 뜨고 누르면 다음 뽑는 사람으로 넘어가는지; (c) `finish_agent_turn`은 그 줄로만 뜨고 `#actions`에는 없으며 한 번 눌러 `/actions` 요청 하나로 끝나고 다시 붙잡히지 않는지; (d) `finish_reveal`도 마찬가지이고, 라벨은 살 수 있는 카드(`isAcquire`)가 legal action에 남아 있는 동안 "구매 끝 · 턴 종료 ▶"이고(사기 전부터 그렇다) 더 살 게 없으면 그냥 "턴 종료 ▶"인지 — 산 것으로 정해지지 않는다; (e) 되돌릴 수 없는 수로 끝난 턴도(`summary.undo`에 그 좌석 항목이 없어도) 같은 줄이 뜨고 되돌리기 줄은 없는지; (f) `pass_combat_intrigue`는 "패스 · 턴 종료 ▶"이고 한 번 누르면 넘어가는지. (c)~(f)는 한 판(seed 11 — turn_controls.py가 이미 `deploy_troops`가 여럿인 것을 확인한 시드, Combat Intrigue 참가에도 필요) 안에서 기회가 될 때마다 검사하고, 리더 드래프트의 두 seed는 `POST /games`를 seed마다 직접 쳐서(브라우저 없이) `first_player`로 찾는다. 이어 영어에서는 홀드 한 번과 명시적 종료 결정 한 번에서 그 줄의 문구가 항상 "End turn ▶"로 끝나고 한글이 하나도 없는지 본다. 끝으로 (g) `pass_endgame_intrigue`는 "패스 · 턴 종료 ▶"이고 한 번 누르면 넘어가는지 — 리더 드래프트를 켠 기본 게임에서 좌석 0이 첫 합법 행동만 고르는 정책으로 라운드 별 확인 없이 raw HTTP로(브라우저 없이) seed를 찾은 뒤 그 seed로 브라우저를 그대로 재생한다; 못 찾으면 건너뛰지 않고 그냥 뺀다. |
| `board_tokens.py` | 보드 스캔의 **인쇄된 자리**에 놓이는 조각(2026-09-20). 새 판에서 Alliance token 넷이 각 진영 strip의 점선 원을 정확히 덮는지(`catalog.tracks.influence.alliance`·`alliance_size`), Control·bonus spice·Maker Hooks는 아직 없는지. 칸 hotspot 22개가 카탈로그의 `box`(그 칸이 그림 둘레에 인쇄한 **흰 테두리**, 2026-09-21)와 같은 사각형이고 테두리 윤곽(`.space-frame`)이 `catalog.space_frame.cut`의 깎인 두 모서리를 갖는지, hover가 상자 전체를 칠하지 않고 윤곽만 밝히는지. 칸의 Agent가 공용 윤곽(`#agent-outline`, 문서에 하나)으로 그린 **Agent 아이콘 실루엣**이고 좌석 색·좌석 이름·번호 없음·아이콘 비율(52:81)을 지키며, 넷까지 테두리 안에 나란히 서는지. 관측소의 Spy가 공용 윤곽(`#spy-outline`)의 **Spy 아이콘 원통**(윗면 타원 포함)이고 좌석 색·이름·번호 없음·비율(56:80)을 지키며, 관측소 점(`catalog.posts`)에 가운데 맞춰 원판 너비(`catalog.post_size`)로 서는지. 한 관측소를 함께 쓰는 Spy는 **먼저 온 Spy 위에 쌓인다**(2026-09-21 밤, 사용자 지적): 로그의 `spy_placed` 순서대로 아래에서 위로, 모두 원판 너비, 한 층이 Spy 높이의 60%(원통의 두 면 중심 간격 48/80), 위 Spy가 아래 Spy 위에 그려지는지, 다시 놓인 Spy가 맨 위로 가는지 — 좌석 순이 아닌 도착 순서를 로그에 넣어 본다(옛 나란히 배치에서는 5개 실패). 몇 라운드를 둬야 나오는 상태라 **페이지의 view를 고쳐** 다시 그린 뒤(그리기의 입력은 view뿐이다) 브라우저가 실제로 배치한 사각형을 카탈로그 표와 대조한다: Control marker가 세 칸 아래 인쇄된 깃발의 상자와 같고 좌석 색인지, bonus spice 육각형이 인쇄된 Maker 육각형을 덮고 수량을 적는지(0이면 없음), Maker Hooks token이 네 garrison의 슬롯을 채우는지(그림은 돌리고 뒤집어 놓는다), 보유된 Alliance token은 상태창에 중복하지 않고 보드의 보유자 색 고리로 표시되는지. 살아 있는 판에서는 `reveal_turn`의 `reveal_preview`(서버 dry run)가 손패 옆 "지금 공개하면"과 Reveal 버튼에 뜨는지, Intrigue discard가 카드 줄 대신 **한 줄**이고 누르면 discard(최신 먼저)·trash 목록이 뜨는지. 끝으로 **Bloodlines 판**(2026-09-21): 4인 setup이 사다우카 지휘관을 여섯 칸(Sardaukar·Dutiful Service·Deliver Supplies·High Council·Gather Support·Assembly Hall `[Bloodlines p. 3]`)에 두는지, 각 칸에 지휘관이 하나씩 서 있고 그것이 룰북 그림(`catalog.commander_token`)인지, 받침이 흰 테두리의 오른쪽 위 기준점(`catalog.commander_spot`)에 오고 키가 테두리만 하며 테두리의 나머지는 에이전트 자리로 비어 있는지, 그림의 비율, 칸 클릭을 가로채지 않는지, 칸 이름(aria-label)이 지휘관을 말하는지, view에서 한 칸을 빼면 그 지휘관이 사라지는지, 그림이 없으면 같은 자리에 그린 표시가 서는지. 옛 클라이언트(빨간 C 배지)에서는 8개가 실패한다. |
| `leader_card.py` | 리더 카드 자체에 좌석 자신의 상태를 그리기(2026-09-25). 좌석 패널의 리더 팝오버(썸네일·이름, hover·pin)가 이제 리더 그림 위에 그 좌석의 상태를 그린다: Feyd-Rautha의 훈련 트랙 칸(`FEYD_TRAINING_TRACK`)과 Chani의 전술 트랙 칸(`tactics_track_space`, `display.leader_layout`이 잰 인쇄 상자, `catalog.leaders[id].layout`). (a) `board_tokens.py`처럼 **view를 고쳐** Feyd의 모든 트랙 칸과 Chani의 인덱스 0..10을 하나씩 두고 그때마다 좌석의 리더 팝오버를 열어, 측정한 토큰 중심이 카탈로그가 내놓는 상자 안에 있는지, 그림이 그 언어의 파일인지(한국어·영어 둘 다 — 그림은 언어를 따르지만 상자 표는 하나뿐이고 한국어 그림이 몇 픽셀 짧아도 맞는지) 본다. (b) **실제 판** 하나: 고정(비드래프트) setup이 Feyd-Rautha를 좌석 0에 배정하는 seed를 raw HTTP로(브라우저 없이, `turn_end.py`의 `find_draft_seed`와 같은 기법) 찾은 뒤 그 seed를 브라우저로 그대로 재생해, 토큰이 (편집이 아니라) 서버가 실제로 보낸 트랙 칸에 서는지 본다. (c) Lady Jessica의 뒤집힌 면: `leader_face_id`를 `reverend_mother_jessica`로 고친 view가 그 면 자신의 그림을 보이는지(뒤집기 자체는 이미 `panels.js`의 `leader_face_id \|\| leader_id`로 끝까지 연결돼 있었다 — 새 stage 래퍼가 그것을 건드리지 않는지만 본다). 덤으로 카드 그림이 없을 때(로컬 에셋 없음)는 stage 대신 좌석 상태 줄과 같은 문구의 텍스트 한 줄만 뜨는지도 본다. (d) Steersman Y'rkoon의 Navigation 카드 네 칸(`YRKOON_NAVIGATION_SLOT_BOXES`, 인쇄된 "1 2 3 4" 띠): 리더 그림 **위**에 뜨는 한 줄로, 카드가 겹치지 않고 각 칸의 가로 중심(폭이 아니라)에 놓이는지 0~4장 played·나머지 remaining의 모든 조합(15가지)에서 확인하고, 같은 조합을 owner 시점(Y'rkoon을 좌석 0에 둔 편집, `view.private.navigation_slots`)과 non-owner 시점(Y'rkoon을 좌석 1에 둔 편집, 같은 좌석-0 시청자의 손대지 않은 실제 `view.private` -- 실제 non-owner의 `view.private`는 항상 있고 다른 좌석의 `navigation_slots`만 안 실릴 뿐이다)으로 각각 보고, 행이 리더 그림 **위**(겹치지 않고)에 있는지도 잰다 — played 칸은 누구에게나 그 카드 얼굴, 아직 안 played인 칸은 owner에게만 자기 얼굴(흐리게, "뒷면" 배지)이고 다른 좌석에게는 신원 없는 빈 칸(뒷면 그림이 없으므로 페이지 자체 팔레트의 빈 상자, `report_assets.md §2`)인지. `navigation_option_wording`은 ITEM 8b의 `play_intrigue` 대응짝을 `play_navigation`에도 채운다(`describeAction`, core.js): 이 액션은 "option"만 갖고 card_id가 없어(`rules/navigation.py`), 아직 뜬 (라이브) 줄은 카드를 `view.private.navigation_slots`의 맨 앞에서, 이미 기록된 줄은 그 step 자신의 `navigation_card_played` 이벤트에서 읽어와 카드 이름 + 그 옵션의 인쇄 문구를 보이는지(`intrigue_options.py`의 합성 액션 기법), 로그 줄(`logEventPayload`)도 `intrigue_played`처럼 option을 더는 되풀이하지 않는지 확인한다. **리뷰(review) 라벨은 그 라이브 폴백을 절대 빌려 쓰면 안 된다**(2026-09-25 리뷰 지적): `_review_step_label`(sessions.py)은 이벤트도 없고 `type: "action"`만 있어 라이브 legal action(그런 키 자체가 없다, `_serialize_action`)과 모양이 다른데, 옛 코드는 `action.type`을 보지 않고 이벤트가 없으면 곧장 `view.private.navigation_slots`로 넘어갔다 — 하지만 리뷰 커서 시점의 `state.view`는 그 step **이후**의 view라서 이미 놀린 카드는 슬롯을 떠나고 다음 카드가 앞에 와 있다: 틀린 카드 이름을 사실처럼 보였다. `{type: "action", actor: 0, action_id: "play_navigation", arguments: {option: 0}}`에 `view.private.navigation_slots`를 다른 카드로 채워 넣고도 여전히 "Option: 0"/"선택지: 0"만 나오는지가 이 회귀 검사다. `review_label_repro`는 같은 버그를 합성이 아니라 **실제 판**으로 재현한다: `game_seed=2`(all-heuristic, Bloodlines + Leader Draft)가 결정적으로 Y'rkoon을 좌석 3에 드래프트시키고, log step 120(`play_navigation`, option 0)이 `navigation_card_9`를 슬롯 1에 놓는데, 리뷰 커서 121(그 step 이후 view)에서는 `view.private.navigation_slots`의 맨 앞이 이미 `navigation_card_1`이다 — `enterReview(3, {cursor: 121})`을 페이지 컨텍스트에서 직접 불러 `#review-status`가 "Option: 0"만 말하고 두 카드 이름 다 안 보이는지(영어·한국어), 같은 step의 **턴 로그 줄**(자기 이벤트가 있어 영향받지 않는다)은 여전히 "Navigation Card 9"를 맞게 보이는지 확인한다(seed 검색이 필요 없었다: raw HTTP로 이 seed가 이 시나리오를 내는지 먼저 직접 확인했다). 끝으로 `live_yrkoon_navigation`은 **`--remote` 서버에 좌석이 다른 두 브라우저 컨텍스트**로 (b)를 재현한다 — open 모드 한 세션의 두 `viewSeat`가 아니라 진짜 두 세션이어야 서버 자신이(클라이언트가 아니라) 다른 좌석을 가리는 것이 증명된다(`_authorize_seat_locked`의 "admin key도 열지 않는다" 주석). 리더 드래프트 First Player가 좌석 1이 되는 seed를 raw HTTP로 찾아(좌석 0이 첫 픽커라 다른 누구보다 먼저 Y'rkoon을 집을 수 있다) 두 좌석을 claim하고 드래프트 + Navigation 4장 배치를 실제로 밟은 뒤, 좌석 0 자신의 페이지는 자기 얼굴들을, 좌석 1 페이지는 빈 칸만 보이고 좌석 1 자신의 `view.private.navigation_slots`는 끝까지 비어 있는지 확인한다. 옛 클라이언트에는 `.popover-leader-stage`도, `catalog.leaders[id].layout`도, `.popover-navigation-row`도, `describeAction`의 `play_navigation` 분기도 없어 첫 대조에서 바로 실패한다(2026-09-25 A/B 확인). **STEP L3**(같은 날, 이어서): Kota Odax의 Secret Project 타일과 Shaddam Corrino IV의 따로 치워둔 사다우카 계약 두 장도 리더 팝오버로 옮긴다 — `kota_secret_project`는 소유자에게는 자기 얼굴(흐리게, "뒷면" 배지), 다른 좌석에게는 신원 없는 `.tile-back` 빈 칸, 타일을 손에 넣으면(`has_secret_project`가 거짓이 되면) 아예 아무것도 없는지 본다(예전에 있던 개인 영역 "peeks" 줄의 두 번째 사본은 제거됨). `shaddam_sardaukar`는 시장 칸의 사다우카 조각(strip)이 아예 없어졌는지(누가 갖고 있든), 그의 팝오버 아래에 계약 두 장이 앞면으로 나란히 뜨는지, 하나를 가져가면 편집한 view를 따라 한 장으로 줄어드는지 확인한다. `status_line_drops_leader_flags`는 좌석 패널의 접는 상태 줄에서 Chani의 Tactics space·Y'rkoon의 남은 매수·Kota의 Secret Project 세 플래그만 빠지고 Piter의 뒤틀린 덱 수는 그대로인지 본다. `leader_popover_settles_within_viewport`는 이 단계의 스크린샷에서 실제로 걸린 회귀를 지킨다: 리더 그림에 `aspect-ratio`가 없어 로드 전에는 팝오버 키를 짧게 재서(`placePopover`) 잘못 놓일 수 있다 — 고친 것은 이미지가 뜰 때마다 `placePopover`를 다시 부르는 것 하나뿐이고(스크래치 A/B로 이 재배치만으로도 모든 이미지가 떴다), Kota의 타일·Shaddam의 계약에 남아 있던 `visualCard`의 기본 `loading="lazy"`도 `eagerCard`(core.js)로 즉시 로드하게 걸어 뒀지만(같은 A/B로는 이것 없이도 다 떴다) 값싼 이중 방어라 남겨 뒀다 — 1366x768에서 가장 아래 좌석(팝오버가 화면 밖으로 밀리기 쉬운 자리)에 두 팝오버를 열어 안정된 뒤 여분 줄이 실제 높이를 갖고 뷰포트 안에 들어오는지 확인한다. **고침 검토에서 나온 블로커**(같은 날, 이어서): 그 `load` 재배치 자체가 회귀를 하나 더 냈다 — `closePopover`는 `pop.hidden = true`만 하고 내용은 지우지 않는데(다음 `openPopover`의 `pop.textContent = ""`에서만 지워진다), 그 안에 아직 로딩 중이던 이미지가 나중에 뜨면 `loadingImage.isConnected`만 보던 핸들러가 이미 닫힌 팝오버를 `placePopover`로 다시 열어(`pop.hidden = false`) `hover` 클래스 없이 좌석 패널·보드 위에 얹혀 다음 클릭까지 포인터를 가로챘다. `popover_stays_closed_after_late_image`는 콜드 캐시 + 느린 네트워크(CDP `Network.emulateNetworkConditions`, latency 1500ms) 컨텍스트에서 hover를 뗀 경우와 pin 후 Escape를 누른 경우 모두, 닫은 지 4.5초 뒤(느려진 이미지가 다 뜬 뒤)에도 팝오버가 계속 닫혀 있는지 본다 — 핸들러에 `!pop.hidden`을 더한 한 줄이 고침이고, 이 줄만 되돌리면 두 경우 다 FAIL로 재현된다. `no_image_fallback`의 `kota`·`shaddam` 갈래는 리더 그림뿐 아니라 Kota의 타일(`tech`)·Shaddam의 두 계약(`contracts`) 자신의 카탈로그 그림까지 지워 두고도 폴백 텍스트 줄 아래에 `visualCard`의 이름 있는 textcard(카탈로그 그림이 아예 없을 때 예전 개인 영역 사본이 하던 일)가 뜨는지 본다 — `leaderSecretProjectBox`/`leaderSardaukarRow`가 `image && seatState` 분기에서만 불려 리더 그림만 없어도(다른 그림은 있어도) 드러나지 않던 것을, 리더 그림이 없는 분기에서도 부르도록 고쳤다. 옛 클라이언트(이 STEP L3가 커밋되기 전의 HEAD)로 전체를 돌리면 STEP L3의 검사 25개가 실패한다(2026-09-25 A/B 확인; `popover_stays_closed_after_late_image` 자체는 STEP L3 안에서만 있는 코드를 검사하는 회귀라 HEAD에서는 자명하게 다 통과하지만, 고치기 전 그 한 줄로 따로 되돌려 보면 두 경우 다 FAIL로 재현된다). `card_click_inside_leader_popover`와 `pinned_popover_survives_rerender`는 팝오버를 붙인 기준 요소가 페이지에서 사라졌을 때 팝오버가 왼쪽 위 구석으로 튀지 않는지 본다: 고정된 Shaddam 팝오버 안의 계약 카드를 누른 경우(그 카드는 새 내용으로 바뀌며 지워진다), 느린 네트워크에서 좌석 카드 칩을 고정한 채 남의 수로 좌석 패널이 다시 그려진 뒤 그림이 늦게 뜬 경우 — `placePopover`가 떨어진 기준 요소면 제자리를 지키게 한 고침만 되돌리면 이 둘이 FAIL이다. `ability_names_once`는 Bloodlines 리더(능력 문구가 이미 능력 이름으로 시작한다)의 팝오버가 "Tactician: Tactician"·"Plot Course: Plot Course"처럼 이름을 두 번 쓰지 않는지 본다. |
| `spectate.py` | 전 좌석 AI 게임의 관전(2026-09-20). 그런 게임은 앉을 좌석이 없고 서버가 생성 요청 안에서 끝까지 둬 버리므로, 페이지는 그것을 **첫 상태에서 시작하는 리플레이 검토 + 자동 재생**으로 연다(기본 한 턴씩 · 1초). 커서를 페이지의 turn stop 표(`state.review.stops`)와 대조한다: stop마다 두 턴 사이인지(앞 행동이 턴을 닫았거나 다음 행동의 좌석이 다름), 재생이 **모든 stop을 차례로, 약 1초 간격으로** 밟는지, 행동 로그가 커서까지만 보이고 방금 더해진 카드가 fresh인지, 상태 줄이 그 턴을 연 행동과 "외 N수"를 말하는지, 헤더가 최종이 아니라 화면의 라운드·phase를 말하는지, 좌석들이 잇따라 Combat/Endgame Intrigue를 패스해도(`pass_combat_intrigue`·`pass_endgame_intrigue`) stop이 그 사이에 끼지 않고 하나로 묶이는지(2026-09-24). 이어 조작: 일시정지, 한 수씩 · 0.25초, 손으로 넘기면 재생이 멈추고 탐색(슬라이더)은 멈추지 않는지, 검토 좌석을 바꿔도 위치와 재생이 유지되는지(AI 좌석의 손패를 "내 손패"라 부르지 않는다), 끝에 닿으면 멈추고 최종 순위가 뜨며 재생 버튼이 "처음부터 재생"이 되는지, 검토 종료 뒤 "AI 대국 다시 보기"와 새로고침이 다시 처음부터 트는지. 사람 좌석이 있는 게임은 혼자 재생되지 않아야 한다. |
| `endgame.py` | 끝난 판(2026-09-20). 전 확장 + 전 좌석 AI 판은 생성 요청 안에서 끝나므로 그 상태가 싸다. **최종 순위표의 Garrison 칸이 동점 판정이 쓴 수와 같은지**(`rules/endgame.py`는 `troops_garrison + commanders_garrison`으로 순위를 매긴다 — OQ-047), 지휘관이 낀 행은 title에 내역을 적는지, 1등 행만 winner인지. 종료 후 공개(OQ-010 판정 4)가 좌석마다 절과 Hand·Deck 순서·Intrigue·공용 덱·Contract bank를 내놓는지. 마지막으로 같은 판을 **노트북 1366x768**에서 열어 결과에 스크롤로 닿는지 — 스크립트가 두 번째 뷰포트를 쓰는 첫 사례다. |
| `narrow.py` | 1100px 미만 레이아웃(2026-09-20). `#center`가 보드와 공용 카드를 위아래로 쌓고, `#market`이 행이 되고, strip의 카드 줄이 줄바꿈을 멈추는지. **일부러 남긴 비-발견**: 그 줄은 가로 스크롤이 허용되지만(`overflow-x: auto`) 1090·1000·820·700px 어디서도 실제로 넘치지 않는다(`#market`이 wrap이라 strip이 자연 너비를 갖는다). 그래서 보존할 오프셋이 없고, 검사는 그 상태가 **도달 불가**임을 단언한다 — 나중에 넘치게 되면 이 검사가 실패해 알려 준다. 이어 **머리글**(2026-09-24, 반쪽 노트북 창): 상태 줄이 헤더의 고정 44px 안에서 세 줄로 줄바꿈되며 라운드가 위로 잘리던 것(683px 한글에서 top -9.8px, 영어는 라운드가 화면 밖). 관전 판과 실제 판 × 두 언어 × 600·683·720·768·1366px에서 헤더가 44px 한 줄인지, 상태 줄이 헤더 안에 있는지, 라운드·단계(`.status-core`)와 검토 라벨(`.status-label`)이 잘리지 않고 온전한지(부모 상자와 화면 안, 제 글자폭 이하), 페이지가 옆으로 스크롤되지 않는지, 전체 문구가 title에 있는지, 1366px 한글 실제 판에서는 배지도 잘리지 않는지. 줄어드는 것은 시드·확장 배지(`.status-badges`, 말줄임)와 그다음 제목뿐이다. 옛 클라이언트에서는 한 줄·라운드·라벨·title 검사가 좁은 너비마다 실패한다. |
| `columns.py` | 공용 카드 열 접기(2026-09-20). 전 확장이면 열이 여덟 개다. 각 열이 자기 제목에서 접히는지, `c`가 전부 접었다 펴는지, 접힌 열이 여전히 목록에 남는지, 남이 한 수 둬도(=`#market` 재생성) 접힘이 풀리지 않는지, 새로고침 뒤에도 기억되는지, 입력란에 친 `c`는 그냥 글자인지. 다 접으면 열이 제 너비를 돌려주는지(190→170px; 보드는 그만큼만 큰다 — 보드 크기는 센터 그리드가 정한다). 그리고 **Bene Tleilax board를 크게 보기**: 열 안은 31배 축소지만 크게 열면 5.6배로 본 보드(10.4배)보다 덜 줄고, 연구 칸이 4배 이상 커지고, Escape·배경 클릭으로 닫히며, 남이 한 수 둬도 열린 채 유지되는지. |
| `seats.py` | 좌석 패널 접기(2026-09-20). 좌석 넷이 카드 줄을 "자세히"로 접는지, 접혀도 무엇이 들었는지 말하는지, 한 좌석만 펴도 나머지는 그대로인지, 재렌더·새로고침을 견디는지, `s`가 전부 펴고 접는지, 접은 상태가 실제로 칸 높이를 줄이는지(후반 2,189 → 936px). 존 수치 줄이 영어로 되돌아가지 않았는지도 본다. 보드에서 바로 보이는 영향력·주둔지·교전 병력·전투력·연구/틀레이락스 위치와 병력 공급처는 상태창에 중복하지 않고, 표본은 아이콘과 수량으로 보이는지도 확인한다. 좌석 패널의 버림 더미를 눌러 여는 팝오버 제목(ITEM 8f, 2026-09-25 — `${t("common.seat", {seat})} discard`로 이어 붙여 한글 좌석 이름 옆에 영어 낱말이 그대로 남았고, 첫 수정은 좌석 패널만 따로 `mine`을 판정해 관전 중(전 AI 판을 검토, `mySeats() == []`)인데도 보고 있는 AI 좌석의 더미를 "내 버림 더미"라고 불렀다)도 매 좌석마다 본다: 라틴 낱말이 없는지, 그리고 이 관전 시나리오에서는 어느 좌석도 "내 버림 더미"가 아니라 손패 줄과 같은 "{좌석}의 버림 더미" 문구여야 하는지(`mySeats()`가 실제로 빈 배열인지부터 확인). 이어 **노트북 1366x768**(2026-09-21): 같은 판의 25/50/75/95% 네 시점 모두 접힌 좌석 넷이 칸에 들어오는지(칸이 519~652px로 바뀐다 — 손패 줄이 두 줄이 되면 줄어든다; 옛 패널은 703~738px), 접기가 머리 줄 끝의 화살표이고 누를 만한 폭인지, 그림이 빠진 자리에서도 지도자 이름이 보이는지, 화살표가 펴고 접는지. 1600x1000에서는 지도자 그림 넷이 그대로 있는지도 본다(높이 960px 이하에서만 압축). |
| `help.py` | 도움말과 접근성(2026-09-21). 전에는 클라이언트에 `aria-live` 영역이 하나도 없어 차례 변경도 오류도 스크린 리더에 읽히지 않았고, 아이콘만 있는 컨트롤(검토 막대의 ⏮ ◀ ▶ ⏭, 좌석 토큰)에 이름이 없었고, 아이콘을 설명하는 곳이 없었다. 알림 영역이 polite·atomic이고 `display:none`이 아니라 시각적으로만 숨었는지, 도착 때와 **결정 좌석이 바뀔 때마다** 그 좌석을 말하는지, 오류 줄이 `role="alert"`·연결 알림이 `status`인지, 검토 버튼과 좌석 토큰의 이름. 도움말은 한글 자판이 보내는 `?`(key `?`, code `Slash`)로 열리고 라벨 붙은 modal이며 포커스가 들어가는지, **좌석 패널과 손패에 실제로 그려진 모든 아이콘이 범례에 있는지**(렌더러를 다시 돌리는 게 아니라 교차 확인), 좌석 표시(`C2`·`1st`)와 단축키 넷, 열린 동안 `c`가 뒤에서 동작하지 않는지, Esc·배경 클릭으로 닫히고 포커스가 제자리로 돌아오는지, 입력란의 `?`는 글자인지. 이어 ITEM 8e — **노트북 1366x768**(2026-09-25, 한글·영어 모두): 한 턴 진행 절(제목 h3 + 목록)이 아이콘 범례보다 앞, 패널 머리글 바로 뒤로 옮겨졌는지, 열릴 때 스크롤 없이(`scrollTop 0`) 그 절 전체가 `#help-body`의 보이는 범위 안(목록 아랫변 ≤ `clientHeight`)에 들어오는지. |
| `lang.py` | 언어 전환(2026-09-21). 인쇄 텍스트와 공간 이름은 두 언어 모두 영어이고 카드·리더·분쟁·계약·기술 타일·지휘관 기술 토큰 이름은 한글판 인쇄가 확인된 것만 한국어이므로(2026-09-23), 영어에서는 **페이지에 한글이 하나도 없어야** 한다(예외는 다른 언어를 그 언어로 부르는 전환 버튼뿐). 전 확장 seed 7 판에서 텍스트 노드와 title·alt·aria-label·placeholder·문서 제목을 전부 훑는다: live 테이블(몇 수 둔 뒤 포함), 도움말, 끝난 판의 검토 끝·열어 둔 disclosure·종료 배너, 설정 화면. 한국어 → 영어 → 한국어 왕복 뒤 같은 위치의 좌석·배너·로그·공용 카드 텍스트가 **그대로** 돌아오는지, 새로고침 뒤에도 영어인지, 한국어에서는 엔진의 영어 prompt가 번역되고 영어에서는 보낸 그대로인지, 라벨 테이블이 한국어로 되돌아오는지. 이 검사가 찾은 것: 전환 전 문장을 들고 있던 알림 영역, 검토 좌석 선택지, 숨은 설정 화면의 좌석 선택, 취소되지 않은 알림 타이머. **한국어 쪽**(2026-09-22): 새 테이블·열두 수 뒤·도움말·검토 끝·설정 화면에서 같은 표면을 훑되, 카탈로그의 **이름**(`name`·`ability`·`signet` 필드 — 엔진 id는 빼지 않는다), 인쇄 텍스트(`iconize()`가 두르는 `.card-text`), 도움말 범례의 영어 병기, `KOREAN_KEEPS_ENGLISH`와 스크립트의 `KOREAN_CHROME_ENGLISH`(`train extra`·AI·Esc; 지도자 약칭은 2026-09-23 한글 이름으로 바뀌어 뺐다)를 지운 뒤 **라틴 낱말이 하나도 남지 않아야** 한다. 훑는 동안 좌석 세부와 행동의 ⓘ 세부를 모두 편다(좌석 상태 줄의 Family Atomics가 접힌 세부 안에 있었다). 옛 클라이언트(`d09532c`)에서 헤더의 `seed … · CHOAM · promo · Bloodlines · Tech · Immortality · draft`, `황제 Alliance`, `heuristic`, Shield Wall·게임판 alt, Family Atomics, Gather Intelligence, Infiltrate, Endgame, 전환 뒤 영어로 남던 검토 상태 줄로 실패한다. 검토 끝에서는 좌석을 펴고 원로회·소드마스터가 한국어로 나오는지(그런 좌석이 실제로 있는지도), 탭과 헤더가 "듄 임페리움: 봉기"인지 본다. **카드 이름·그림**(2026-09-23): 새 테이블의 공용 카드 열에서 `name_ko`가 있는 카드마다 제목(`title`)이 한국어에서는 한글 이름, 영어에서는 영어 이름인지, `image_ko`가 있으면(에셋 체크아웃이 있을 때) 그림도 그 언어의 파일인지 본다 — 예비 카드 2장이 늘 있어 에셋 없이도 이름 검사는 대상이 있다. |
| `log_follow.py` | 실시간 행동 로그의 자동 추적(2026-09-24, 감사가 재현: 라운드 7에서도 로그가 "#2 지도자 선택"에 멈춰 있고 새 카드는 25,000px 아래). `renderLog()`는 "따라가는 중"을 이전 목록의 스크롤 위치만으로 판정했는데, 렌더는 항상 새로 온 첫 카드로 스크롤하지 끝으로 가지 않는다 — 그 묶음이 목록보다 크면(지도자 드래프트, 라운드 전환) 다음 렌더는 독자가 "끝에서 멀다"고 읽고 그 낡은 위치를 영원히 유지한다. 전 확장 + 지도자 드래프트로 사람 좌석 0을 1440x900에서 최소 3라운드 두면서(`applyAction`/`confirmTurn`으로, open_mode.py와 같은 방식), 항목이 늘 때마다 그 항목을 담은 첫 카드(ARRIVED 카드, ITEM 8a부터는 `.turn-card.fresh`가 아니다)의 위가 `.log-list` 뷰포트 안에 있는지 `getBoundingClientRect`로 잰다 — 지도자 드래프트의 큰 묶음이 이 검사를 최초로 시험한다. 이어 목록을 중간 위치로 스크롤해 두고 한 수를 두면 오프셋이 정확히 그대로인지(수동으로 읽던 자리 보존, 바뀐 적 없는 동작), 끝으로 스크롤해 두고 한 수를 두면 다시 따라가는지 본다. 옛 클라이언트에서는 지도자 드래프트 직후 라운드 1부터 라운드 3 끝까지 58개 검사가 실패한다(카드가 목록 뷰포트 밖 최대 12,344px). |
| `log_words.py` | 행동 로그가 엔진 id가 아니라 말로 쓰이는지(2026-09-21). 끝난 판 넷(heuristic seed 7, random seed 25·30·23 — 영향력 교환·Secrets 훔치기·지도자 드래프트가 나오는 seed)의 로그 전체를 페이지 자신의 함수(`turnLine`·`logEventLine`·`chanceLine`·`describeReviewStep`)로 두 언어에 걸쳐 그리며 `prettify()`를 감싸 둔다: 표가 모르는 id의 마지막 수단이라 **한 번이라도 불리면 누출**이다. 한국어에서는 카탈로그 이름과 용어집이 일부러 영어로 둔 구(Secret Project·Usurp 등, 스크립트의 `KOREAN_KEEPS_ENGLISH`) 밖에 라틴 낱말이 없는지, 영어에서는 한글이 없는지, 두 언어 모두 엔진 id의 모양(snake_case, `a:b:c` 경로, 연구 좌표 `c2r2`, 관측소 id와 그 prettify형)이 없는지 본다. `pass_combat_intrigue`·`pass_endgame_intrigue` 줄마다 muted 처리(`quiet` class, panels.js `QUIET_ACTIONS`)가 실제로 붙는지도 본다(2026-09-24). 이어 첫 판의 검토 끝 화면 전체를 좌석 세부를 펴고 title까지 훑는다. 판들이 이 검사가 지키는 자리(무작위 섞기, 관측소 인자, 연구 칸, Feyd 트랙, 스킬 인스턴스, Family Atomics의 치운 카드, 영향력 교환, Secrets 훔치기)에 닿지 않으면 빈 말뭉치로 통과하지 않고 실패한다. 옛 클라이언트에서는 6개 검사가 실패한다(`Count: 2`, `c1r3`, `imperium:blank_slate:0,…`). |
| `log_passes.py` | 이어진 Combat/Endgame Intrigue 패스가 로그 카드 하나로 접히는지(ITEM 8h, 2026-09-25 — 감사가 재현: 전 좌석 AI 판을 끝까지 훑으니 이어진 패스마다 각자 전체 카드 하나씩 차지해 로그 카드의 14-19%, 448px 로그에 라운드당 3-4장(206-300px)이었고 카드마다 "패스"를 두 번(머리 줄의 `#116 패스`와 muted 이벤트 줄) 말했다). 좌석 순서 대조는 페이지의 `logGroups()`를 부르지 않는다 — 접힘을 묶는 join 분기를 꺼도(모든 패스가 제 카드 하나씩을 다시 차지해도), 자기 카드를 닫는 패스까지 접는 조건을 빼도, `logGroups()` 자신에게 묻는 대조는 그대로 통과해 버리는 것을 뮤테이션으로 확인했기 때문이다(2026-09-25 검토). 대신 `runs_of`가 원본 로그(`state.review.meta.log`)에서 같은 접힘 규칙을 독립적으로 다시 구해, heuristic seed 7의 기본판과 전 확장판을 끝까지 두고 두 언어에서: 그 규칙이 찾은 이어진 패스 묶음마다 `.turn-card.passes`가 정확히 하나이고 그 좌석 순서·개수가 그 묶음의 행위자와 같은지, 접힌 카드 중 하나 이상이 두 좌석 이상을 묶는지, DOM에서 바로 이웃한 접힌 카드끼리 같은 action id를 공유하지 않는지(접힘이 꺼졌을 때 남는 모양), 패스를 뜻하는 낱말이 그 카드에 정확히 한 번만 나오는지, 다른 어떤 `.turn-card`도 패스 한 줄만 든 채로 남지 않았는지(옛 버그의 모양), 한국어 카드에 카탈로그 이름·용어집이 영어로 둔 구 밖의 라틴 낱말이 없고 영어 카드에 한글이 없는지 본다. 판이 접힐 패스에 닿지 않으면 빈 말뭉치로 통과하지 않고 실패한다. 옛 클라이언트에서는 `.turn-card.passes`가 없어 판·언어마다 첫 대조("좌석이 일치")부터 실패한다(4개). |
| `remote_fresh.py` | 로그 카드가 빛나는 기준(ITEM 8a, 2026-09-25 — 감사가 재현: 원격 판에서 상대 셋이 차례로 두면 내 차례가 돌아왔을 때 마지막 상대 한 수만 빛나고 그 앞 두 수는 빛나지 않았다). `renderLog()`는 "이전 렌더 이후 도착"이라는 숫자 하나를 빛남과 스크롤 두 용도에 같이 썼는데, 로컬에서는 AI 전체 묶음이 한 렌더로 오니 둘이 늘 같았지만 원격에서는 상대 한 수마다 렌더가 따로 나 그 숫자가 매번 마지막 한 수로 리셋됐다. 고친 뒤에는 **빛남**(`glowFrom`, `ownGlowFrom`)이 "보는 좌석 자신의 마지막 수 이후"를 로그 전체에서 다시 구하고, **스크롤**(`arrivedFrom`)은 옛 "이전 렌더 이후" 그대로 남는다. (a) `--remote` 서버, 좌석 0은 실제 브라우저, 좌석 1~3은 각자의 좌석 쿠키로 raw HTTP를 두어(카드를 보는 브라우저 없이, 그래서 상대의 한 수 한 수가 그 자체로 doorbell 렌더가 된다) 좌석 0의 차례가 여러 번 돌아올 때마다 빛나는 카드 집합이 정확히 "좌석 0의 마지막 수 이후에 쓰인 카드"(상대 셋의 그 사이 모든 수 + 그 사이 중립 카드, 그 앞은 전혀)와 같은지 — 옛 클라이언트에서는 그 앞에 상대의 수가 있었던 좌석 0의 turn start마다 이 대조가 실패한다(`actual`이 마지막 상대만 남기고 그 앞을 빼먹는다; 원격 서버는 시드를 감추므로 좌석 순서가 무작위라 좌석 0이 먼저 두는 turn start는 기대치 자체가 빈 채로 우연히 통과할 수 있어, 확인한 turn start 중 하나 이상은 다른 좌석 둘 이상의 수가 빛나야 한다는 조건을 따로 강제한다). (b) 기본(open) 서버, 좌석 0 사람 + heuristic 셋: 좌석 0의 턴을 끝내는 수(`finish_agent_turn` 등 명시적 종료를 우선 골라, 같은 응답 안에 AI 좌석들의 수가 같이 오게 만든다)를 두면, 그 렌더에서 좌석 0 자신의 카드는 더 이상 빛나지 않고(옛 클라이언트는 여기서도 빛났다) 뒤이어 답한 AI 카드들은 빛나는지. |
| `combat_result.py` | 전투 결과 줄(UI ITEM 5, 2026-09-24). 전투 해결 뒤에는 화면 어디에도 승자·순위·내 보상이 안 떴다(바로 다음 결정으로 넘어가고, 보상을 고르는 배너 문구도 그것이 전투 보상이라 말하지 않았다). 사람 좌석 0 + 휴리스틱 셋으로 기본판(seed 5)과 전 확장판(seed 2) — 둘 다 라운드 1의 유일한 Tier ONE 교전이 좌석 0이 1위로 끝나 영향력을 직접 고르는 결정 프레임이 열리는 seed다(2026-09-24) — 을 1440x900에서 첫 전투 해결까지 둔다. 배너의 `.combat-result` 줄을 **state.log의 원시 이벤트에서 이 스크립트가 따로 재구성한 값**(렌더러의 함수를 부르지 않는다)과 대조한다: 교전 이름, `combat_reward_gained`가 말하는 순위마다의 좌석(동점은 한 줄에), 내 몫이 그 이벤트 payload와 맞는지(Victory Points 필드 포함 — 그 필드를 이벤트에 채우는 쪽은 `rules/combat.py`의 별도 pytest가 덮는다; Tier ONE 교전은 설계상 Victory Points가 없어 이 값은 늘 0이다). 좌석 0이 순위 밖이면 줄이 그렇게 말하는지. 두 언어에서 각각 이 대조를 반복한 뒤(영어는 한글 0, 한국어는 교전 이름 밖에 라틴 낱말 0), 해결부터 좌석 0의 다음 턴(`agent_turn`/`reveal_turn`/`play_turn_start_card`)까지 — 영향력을 직접 고르는 동안을 포함해 — 줄이 계속 떠 있다가(그 사이 보상 선택을 실제로 했는지도 확인한다) 그 행동 직후 사라지는지 본다. 게임이 끝난 뒤에는 종료 제목이 대신하므로 줄을 띄우지 않는다. 옛 클라이언트에서는 줄 자체가 없어 10개 검사가 실패한다. |
| `intrigue_options.py` | Intrigue 카드 한 장이 선택지를 둘 이상 가질 때 그 행 문구(ITEM 8b, 2026-09-25 — "Special Mission, 선택지: 0" / "선택지: 1"처럼 카드 이름 뒤에 엔진의 숫자 인덱스만 붙고 ⓘ 팝오버만이 둘을 구별했다: `describeAction`의 숫자 인자 분기가 `option`도 그냥 `${label}: ${value}`로 찍었기 때문). 기본 게임(확장 없음, 지도자 드래프트 없음)에서 좌석 0을 고정 시드로 고정 정책(`rng = random.Random(SEED)`가 매 결정 `state.actions.actions`에서 고르고 hold는 바로 confirm)으로 두어 실제로 Combat 결정 하나가 같은 카드의 선택지 0과 1을 동시에 제시하는 상태(오늘은 "Tactical Option")에 닿을 때까지 둔다(SEED는 브라우저 없는 raw-HTTP walk로 한 번 찾아 박아 둔 값 — 같은 정책을 이 스크립트가 그대로 다시 돈다). 그 두 행 각각을: 화면에 있는지, `describeActionText`가 "선택지:"·"Option:"을 안 읽는지(전체 play_intrigue 행에 대해서도), `catalog.intrigue[<key>].text[option]`에서 타이밍 접두("Plot — " 등)를 뗀 문구를 `iconize()`로 옮긴 값과 끝이 같은지, 카드 이름이 여전히 보이는지, 두 행의 문구가 서로 다른지 본다. 그 행을 두었을 때 로그에 그려질 `intrigue_played` 이벤트 줄(`logEventLine`)에도 "선택지:"/"Option:"이 없는지 본다. 언어를 바꿔도 같은 결정이 그대로 있는지 다시 확인한다. 옛 클라이언트에서는 이 검사들이 실패한다(두 행 모두 "선택지: N" / "Option: N"으로 읽힌다). |
| `trash_zone.py` | 이름이 같은 트래시 후보가 보는 좌석의 서로 다른 보관 구역에 있을 때 그 행 문구(ITEM 8c, 2026-09-25 — Feyd 트랙 트래시·Desert Tactics 트래시·교전 보상 트래시(`leader_abilities.py`·`board_effects.py`·`combat.py`, 셋 다 `(*hand, *discard_pile, *in_play)` 순으로 후보를 낸다)가 핸드 사본과 버림 더미·플레이 영역 사본을 똑같이 "카드 {trash} (...) — 단검"으로 찍어, 핸드 사본을 트래시하면 이번 라운드 그 카드를 못 낸다는 차이를 말해 주는 게 없었다). `appendActionItems`(`render.js`)가 이제 한 결정의 행 목록 안에서 action_id와 카드(baseId)가 같으면서 보는 좌석의 서로 다른 구역(`ownCardZone` — 핸드는 `state.view.private.hand`, 버림 더미·플레이 영역은 `state.view.players[seat]`)에 있는 행마다 그 구역을 용어집 낱말({hand}·{discard_pile}·새 {in_play}, `[Main p. 20]`)로 덧붙인다(`zoneSuffixNode`); 한 구역을 공유하는 사본은 서로 바꿔써도 되므로 그대로 둔다. 지도자 드래프트를 끈 기본 게임 seed 0에서 "좌석 자신의 첫 합법 행동을 확정까지 바로 씀"이라는 고정 정책(raw-HTTP walk로 한 번 찾음)이 106번째 결정에서 Feyd Rautha Harkonnen의 Personal Training 트랙 트래시를 "Prepare the Way" 예비 카드의 핸드 사본과 플레이 영역 사본으로 동시에 제시하는 상태에 닿을 때까지, 같은 정책을 브라우저로 그대로 다시 돈다. 그 두 행 각각을: 화면에 있는지, 구역 접미사(" (핸드)"/" (플레이 영역)")로 끝나는지, 카드 이름이 여전히 보이는지, 접미사를 떼면 두 행이 정말 똑같이 읽히는지(ITEM 8c가 보고한 버그 그대로), 화면의 전체 문구는 서로 다른지 본다 — 구역 판정과 baseId는 `state.view`/`state.actions`에서 이 스크립트가 따로 다시 구해(새 `ownCardZone`을 부르지 않아) 옛 클라이언트에서도 같은 결정을 찾아내고, 구역 낱말도 `docs/rules/glossary-ko.md`에서 직접 박아 둔다(옛 클라이언트에는 `in_play` 용어 자체가 없다). 언어를 바꿔도 같은 결정(같은 action index)이 그대로 있는지, 영어에는 한글이 없는지도 본다. 옛 클라이언트에서는 두 행이 정말로 똑같이 찍혀 구역 접미사·행 구별 검사 6개가 실패한다. |
| `save_delete.py` | 저장 목록의 삭제 두 번 누르기 확인(ITEM 8d, 2026-09-25 — "삭제" 버튼이 "불러오기" 바로 옆에서 한 번에 지웠는데, 원격 첫 판에서 호스트가 크래시 뒤 복구하는 자리가 바로 이 목록의 자동 저장 항목이라 오클릭 한 번이 유일한 복구 지점을 날렸다). open 서버(`/saves`에 admin key가 필요 없다)에서 사람 좌석 하나짜리 판을 셋 만들어 각각 이름 붙여 저장한 뒤(SaveAlpha/Bravo/Charlie), 첫 항목의 삭제 버튼이 제 class(`.save-delete`, `불러오기`와 구별)를 가지는지부터: 한 번 클릭하면 `armed` class와 "정말 삭제?"/"Delete for good?" 문구로 바뀌지만 그 자리에서 목록은 그대로이고, 4초(screens.js `SAVE_DELETE_ARM_MS`)를 넘기면 아무것도 지우지 않은 채 "삭제"로 되돌아가는지; 다른 항목의 삭제를 누르면 무장 중이던 버튼이 즉시 풀리는지(동시에 하나만 무장), 화면의 다른 곳(설정 제목)을 눌러도 풀리는지; 더블클릭 한 번은 무장만 하고(0.4초 안의 두 번째 클릭은 같은 동작으로 본다, `SAVE_DELETE_GUARD_MS`), 그 뒤 4초 안에 한 번 더 누르면 그제서야 서버 목록(`/saves`)과 디스크 파일에서 실제로 사라지고 나머지 둘은 그대로인지; 영어로 바꾼 뒤 무장 문구가 "Delete for good?"이고 한글이 없는지(`불러오기`도 그대로 "Load"인지) 본다. 옛 클라이언트에는 `.save-delete` class 자체가 없어(무장 메커니즘이 없다) 21개 검사가 실패한다. |
| `refresh_error.py` | 새로고침 실패 배너가 뒤에 남는 문제(ITEM 8g, 2026-09-25 — 한 snapshot GET이 실패하는 동안 다른 좌석이 HTTP로 한 수를 두면 "게임 상태 조회 실패 (Failed to fetch)"가 뜨는데, 그 뒤 새로고침이 성공해 화면이 서버를 따라잡아도 그 줄이 지워지지 않았다). screens.js `showRefreshError`가 이제 그 배너에 표시(`dataset.source = "refresh"`)를 남기고, session.js `adoptSnapshot`은 그 표시가 아직 남아 있을 때만 새 snapshot을 받을 때 배너를 지운다. [1] 재현 그대로: humans=(0,1) 판에서 snapshot GET 하나를 route로 실패시키고 다른 좌석이 raw HTTP로 한 수를 두면(이미지 다발을 불러오느라 그 요청이 몇 초 늦게 나갈 수 있어 고정 sleep 대신 폴링으로 기다린다) 배너가 뜨는지, 이어서 막지 않은 두 번째 foreign 수가 도착하면 배너가 스스로 지워지는지 — **이 마지막 대조가 A/B 필수 검사**다(옛 클라이언트는 `adoptSnapshot`이 `#game-error`를 건드리지 않아 배너가 그대로 남는다; 2026-09-25 A/B 확인). [2] 내 행동이 낸 오류(POST를 route로 실패시킨 것)는 표시가 없으므로, 뒤이은 무관한 foreign 새로고침(`refresh(null, {foreign:true})`, 서버 상태는 바뀌지 않았다)이 성공해도 문구가 그대로인지 본다. |
| `rehearsal.py` | 실제 원격 판의 리허설(슬라이스 6). `E2E_HOST`에 **이 머신의 Tailscale 주소**를 주면 서버가 그 주소에만 bind하고 브라우저도 그 주소로 들어간다 — 친구들이 실제로 쓰는 origin, 곧 **보안 컨텍스트가 아닌 평문 HTTP**다(`remote.py`·`races.py`·`recovery.py`도 같은 변수로 그 주소에서 돈다; open 서버는 loopback만 되므로 `open_mode.py`는 아니다). (1) `isSecureContext === false`에서 복사 버튼이 링크를 선택해 두는지, 신호음이 예외를 내지 않는지, 좌석·관리자 쿠키가 HttpOnly·SameSite=Strict·게임 경로 한정이고 `Secure`가 **아닌지**(평문 HTTP에서는 Secure 쿠키가 버려진다). (2) 친구 쪽 브라우저를 DevTools 회선 에뮬레이션으로 조여(100 Mbit/s·RTT 20 ms, 10 Mbit/s·RTT 120 ms) 전 확장 게임의 첫 접속 시간·받은 양, 한 수가 상대 화면에 뜨기까지, 새로고침(캐시)을 잰다. 수치는 출력만 하고 실패 조건은 수렴·캐시·오류뿐이다. |

## 새 검사를 더할 때

- 클라이언트 상태는 `page.evaluate`로 읽는다(2026-09-20에 `app.js`를 `labels`·`core`·`screens`·`session`·
  `review`·`render`·`turn`·`board`·`panels`·`app`로 쪼갰지만 전부 classic script라 `state`·`refreshFlight`·
  `doorbell`·`applyAction` 등은 여전히 한 전역 어휘 범위에 있다. `index.html`의 로드 순서가 곧 옛 파일의
  위에서 아래 순서이고, 그 순서가 바뀌면 top-level const가 TDZ에 걸린다).
  화면 문구보다 상태를 단언하고, 문구는 사용자에게 보이는 약속일 때만 본다.
- `page.route`로 클라이언트를 패치하는 검사는 **그 코드가 지금 어느 파일에 있는지** 확인하고 그 파일을
  건다. route handler 안에서 assert가 터지면 요청이 fulfill되지 않아 `goto` 타임아웃으로만 보인다
  (2026-09-20 `races.py --ab`가 이 방식으로 조용히 깨졌다).
- 설정 화면의 확장 체크박스(CHOAM·프로모·혈통·기술·불멸)는 2026-09-22부터 **전부 켜진 채로** 열리고, 기술 모듈
  칸은 혈통이 꺼져 있으면 비활성이다. 더 좁은 판이 필요한 검사는 `common.set_rule_options(page, "bloodlines")`처럼
  남길 칸만 적는다(인자 없이 부르면 기본 게임; async API는 `rule_option_steps()`를 돈다). 이 순서가 기술 칸을
  비활성일 때 건드리지 않게 해 준다.
- `leader_card.py`처럼 특정 지도자가 배정된 seed가 필요하고 미리 알 수 없을 때는 `turn_end.py`의
  `find_draft_seed`처럼 **브라우저 없이 raw HTTP로** (`POST /games` + `GET .../seats/{seat}/view`)
  seed를 하나씩 시도해 찾는다 — 후보마다 브라우저를 띄우는 것보다 훨씬 싸다.
- "N초 기다린 뒤 확인"보다 **수렴 조건을 폴링**한다(`remote.converge`). 멈춤은 타임라인과 함께 실패로 드러난다.
- 경합을 의심하면 추측으로 고치지 말고 `page.route`로 그 순서를 강제해 재현한 뒤 고친다(`races.py`,
  `docs/lessons.md` 2026-09-10·2026-09-11). 동기 API의 route handler 안에서 `time.sleep`하면 그 동안 Playwright의
  다른 handler도 멈춘다 — 두 요청을 서로 다르게 늦추려면 async API를 쓴다(`races.py`의 둘째 실험).
- Playwright는 handler가 선언한 매개변수 개수만큼 인자를 넘긴다. 기본값 매개변수를 둔 handler는 둘째 인자로
  `Request`를 받는다.
